# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-05-19

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->

- **Idioma:** sempre pt-BR nas conversas; código e logs em inglês ok.
- **Build:** valida tudo local antes de pensar em GitHub; não pushar sem confirmação explícita.
- **Estilo de execução:** brainstorming → spec → plan → execute inline com checkpoints. Confirmação por pergunta antes de cada mudança de fase.

## Key Learnings

- **Project:** ai-launcher
- **Description:** Desktop launcher for AI coding CLIs — by Helbert Moura | DevManiac's
- **Validação local v16.0.0 (2026-07-07):** `npm run build`, `npx tsc --noEmit`, `npm test`, `cargo check`, `npm run tauri build`, `npm run e2e` e `cargo test` passaram. Build Tauri gerou MSI em `src-tauri/target/release/bundle/msi/AI Launcher_16.0.0_x64_pt-BR.msi` e NSIS em `src-tauri/target/release/bundle/nsis/AI Launcher_16.0.0_x64-setup.exe`. Vite/Rolldown avisou chunk JS grande (~634 kB min / 181 kB gzip), candidato a code splitting.
- **Code splitting v16.0.0 (2026-07-07):** App.tsx usa `React.lazy` por pagina. `npm run build` reduziu chunk principal de ~634 kB min / 181 kB gzip para ~325 kB min / 102 kB gzip e removeu o warning de chunk >500 kB.
- **Launch flow compartilhado (2026-07-07):** `src/features/launcher/launchSession.ts` e a fonte unica para launches de CLI builtin: LaunchDialog, templates salvos e quick launch. Preserva precedencia env `project > workspace > provider`, registra historico e salva recent/last dir. Nao recriar chamadas diretas a `launch_cli` em UI sem passar por esse helper.
- **Fallback Tauri para QA visual (2026-07-07):** `src/lib/tauri.ts` expoe `isTauriRuntime`/`invokeOrFallback`. Use em leituras nao criticas de boot para evitar erro de `invoke` no Vite/browser; acoes destrutivas/launch ainda podem usar invoke direto e falhar visivelmente se clicadas fora do Tauri.
- **Release v16.0.1 (2026-07-07):** Bump deve tocar package.json/package-lock, src-tauri/Cargo.toml, src-tauri/Cargo.lock e src-tauri/tauri.conf.json. Workflow `release.yml` agora limpa `src-tauri/target/release/bundle` antes do build e usa `docs/releases/<tag>.md` como corpo da release.
- **Release asset audit (2026-07-07):** `scripts/audit-release.sh` deve exigir versao no nome de instaladores/checksums, mas `latest.json` e excecao intencional porque o updater precisa de nome estavel. Nao recolocar validacao de versao nesse filename.
- **Cargo audit v16.0.1 (2026-07-07):** RUSTSEC-2026-0194/0195 em `quick-xml <0.41` foram resolvidos so com lockfile (`plist 1.10.0`, `quick-xml 0.41.0`, `tauri-winrt-notification 0.7.3`). Antes de ignorar advisory transitive de Tauri, tentar `cargo update -p <crate>` nas dependencias intermediarias.
- **Roadmap v20 (2026-07-07):** usuario escolheu trabalhar ate uma mega release publica v20 em vez de publicar v17/v18/v19 separadas. Plano oficial: marcos internos `v20-alpha.N`/`v20-beta.N`, com primeira fase Command Center foundation; docs criados em `docs/PRD-v20.md` e `docs/superpowers/{specs,plans}/2026-07-07-v20-command-os-*`.
- **Command Center v20-alpha.1 (2026-07-07):** nova tab `command-center` e agora a home default apos onboarding. Atalhos numericos mudaram: Command Center `Ctrl+1`, Launcher `Ctrl+2`, Tools `Ctrl+3`, MCP `Ctrl+4`, History `Ctrl+5`, Analytics `Ctrl+6`, Workspace `Ctrl+7`, Doctor `Ctrl+8`, Updates `Ctrl+9`, Prereqs `Ctrl+0`. E2E deve esperar Command Center, nao Launcher, como tela inicial.
- **Command Center launch (2026-07-07):** o botao principal "Lancar workspace" usa `launchCliSession` (nao invoke direto), registra history, usa env/workspace/project profile e mostra toast. "Rodar setup" abre `RunbooksPanel` inline quando ha runbooks; se nao houver, direciona para Workspaces.
- **Command Center readiness (2026-07-07):** readiness cards agora usam sinais reais: Doctor via `check_environment` com `invokeOrFallback`, MCP via `useMcp`/`mcpStore` com fallback seguro, e `.ailauncher.json` via `readProjectProfile`. Ao consumir stores em home/boot, preferir fallback seguro para leituras nao destrutivas.
- **Command Center sessions A4 (2026-07-07):** sessoes ativas vem de `list_active_sessions` com guarda `Array.isArray`; tracked sessions com pid ganham botao Encerrar via `kill_session` + `ConfirmDialog open`. Historico recente ganha Replay usando `launchCliSession`; nunca reintroduzir invoke direto para replay.
- **Project Intelligence v20-alpha.2 (2026-07-07):** detector puro em `src/features/project-intelligence/stackDetector.ts` consome snapshot `{files, manifests}` e detecta Tauri, React/Vite, Node, Rust, Python, Go, Docker, MCP e `.ailauncher.json`, emitindo `primary` + hints de CLI/runbook/tags. Backend Tauri `scan_project_stack(directory)` fica em `commands/cli.rs`, usa `validate_directory`, lista allowlist curta de arquivos e le apenas manifests seguros (`package.json`, `.ailauncher.json`) com cap 256 KiB. Como este crate Tauri nao tem alvo lib, testar comandos com `cargo test <nome>` (sem `--lib`).
- **Profile creator v20-alpha.2 (2026-07-07):** `write_project_profile(directory, contents)` escreve somente `.ailauncher.json` fixo apos `validate_directory`, cap 256 KiB e parse JSON objeto no Rust. Frontend usa `formatProjectProfile`/`writeProjectProfile` em `src/lib/projectProfile.ts`; Command Center gera preview sem `env`/secrets com `cli`, `directory:"."`, provider do workspace e runbook sugerido, e salva so via `ConfirmDialog`.
- **Runbook presets v20-alpha.3 C1 (2026-07-07):** presets locais vivem em `src/features/workspace/runbookPresets.ts`, marcados com tag `preset:<id>` para evitar duplicacao. `RunbooksPanel` recebe `suggestedPresetIds` do Command Center e instala presets sugeridos pela stack detectada; se nao houver sugestao, mostra todos. Comandos precisam obedecer `sanitize_args` do backend (sem `&&`, `;`, pipes, redirecionamento, `$`, parenteses). Para este repo, Node preset usa `npm install --legacy-peer-deps`; Tauri preset usa `--manifest-path src-tauri/Cargo.toml`.
- **Command Center Open IDE (2026-07-07):** quick action `ide` fica no `commandCenterModel` e so habilita com workspace + tool instalado/custom IDE. `launch_tool` agora aceita `directory: Option<String>` e valida quando presente; chamada antiga sem directory continua abrindo no home. `toolsStore` usa `invokeOrFallback` para leituras de boot (`get_all_tools`, `check_tools`) porque Command Center consome tools na home.
- **Runbook conditions v20-alpha.3 C2 (2026-07-07):** `RunbookStep.condition` e opcional e preserva compatibilidade com runbooks antigos. Tipos: `fileExists`, `commandExists`, `envExists`, `previousSucceeded`, `always`. Backend `evaluate_runbook_condition` avalia file/command/env com `validate_directory`, path relativo sem `..`, command sem whitespace e env key via `is_valid_env_key`; `previousSucceeded` fica no frontend porque depende do estado do runner. Runner pula step com status `skipped` quando condicao falha.
- **Runbook execution logs v20-alpha.3 C3 (2026-07-07):** execucoes persistem em `ai-launcher:v20:runbook-executions` via `runbookExecutionStore.ts`, max 50 execucoes, output por step capado em 20k chars. Runner cria `run-*`, atualiza step start/finish/duration/status/output e finaliza com status success/failed/stopped. UI mostra timeline das ultimas 5 execucoes do runbook.
- **MCP por projeto v20-beta.1 D1 (2026-07-07):** `.ailauncher.json.mcp` agora e consumido no Command Center via `src/features/mcp/projectMcp.ts`; IDs aceitam nome simples (`filesystem`) ou qualificado (`codex:github`). Health check roda so para servidores esperados/mapeados do projeto e mostra encontrados/faltando/ok no painel de Project Intelligence.
- **Agent profiles v20-beta.1 D3 (2026-07-07):** perfis de agente sao modelo separado de LaunchProfile/templates. Store em `src/features/agents/agentProfileStore.ts` usa storage registry (`agentProfiles`, `activeAgentProfile`). WorkspacePage faz CRUD/ativacao; Command Center usa agente ativo para sobrescrever CLI/args/provider no launch mantendo `launchCliSession`.
- **Sessions 2.0 v20-beta.2 E1 (2026-07-07):** HistoryPage agora persiste filtros em `ai-launcher:v20:history-filters`, mostra dashboard de active/failures/avg/workspaces/last session, relaciona history com workspace por directory e Agent Profile por cli/provider/args, e replay usa `launchCliSession`. Kill de sessao ativa usa `kill_session` com `ConfirmDialog`.
- **Backup Trust v20-beta.2 E2 (2026-07-07):** `configIO.ts` exporta manifest (`format`, `schemaVersion`, `appVersion`, `exportedAt`, `keys`, `redactedCount`), redige secrets recursivamente e expoe `previewImportConfig(raw)` sem writes. Cuidado: redaction deve mirar nomes de campos secret-bearing (`apiKey`, token, secret, password etc.) sem capturar campos estruturais como `cliKeys`/`providerKey`.
- **Release Trust v20-beta.2 E3 (2026-07-07):** `scripts/audit-release.sh` continua aceitando `latest.json` como nome estavel, mas agora baixa e valida conteudo: `version`, URLs Windows apontando para `/download/<tag>/`, `platforms` e `releaseNotesUrl`. No Windows local, `bash` do PATH pode ser WSL ausente; usar `C:\Program Files\Git\bin\bash.exe -n scripts/audit-release.sh` para syntax check.
- **Release local v20.0.0 (2026-07-07):** manifests versionados para 20.0.0 (`package.json`, lock, Cargo, Tauri), README/README.pt-BR apontam para screenshots v20 e guia `docs/command-os-v20.md`; `npm run tauri build` gerou `AI Launcher_20.0.0_x64_pt-BR.msi` e `AI Launcher_20.0.0_x64-setup.exe`. Smoke test do exe release: ProductVersion 20.0.0, janela viva e 6 WebView2.
- **Release GitHub v20.0.0 (2026-07-08):** publicado via branch `codex/v20-command-os` e tag `v20.0.0`; `main` foi fast-forward para o commit `c861beb`. O primeiro release workflow falhou porque `audit-release.sh` esperava `platforms`; hotfix aceita `downloadUrls` (schema real do workflow) e a reexecucao de `release`/`build` passou. `gh` nao estava autenticado; API publica bateu rate limit durante polling, entao preferir `gh auth login` ou web page/backoff para monitorar Actions.
- **README terminal hero v20 (2026-07-08):** a imagem grande no topo de `README.md` e `README.pt-BR.md` vem de `docs/terminal-hero.svg`. Para validar esse SVG no Windows/Codex, `sharp('docs/terminal-hero.svg').png().toFile(...)` renderizou corretamente; Playwright `page.screenshot` em SVG standalone travou esperando screenshot/fonts.
- **Design QC (2026-07-07):** `openwolf designqc` auto-detectou um servidor errado em `localhost:3000` quando havia outro projeto rodando. Para este repo, usar `openwolf designqc --url http://127.0.0.1:5173` depois de subir `npm run dev`. App Tauri tem `minWidth: 1024`, entao screenshots mobile do navegador servem para stress-test, nao como alvo principal de layout.
- **Antigravity CLI real**: o binário se chama `agy`, NÃO `antigravity`. Instalado por script PowerShell em `%LOCALAPPDATA%\agy\bin\agy.exe`. Não existe pacote npm — `@google/antigravity` retorna 404.
- **Manifesto de update do agy**: `https://antigravity-cli-auto-updater-974169037036.us-central1.run.app/manifests/windows_amd64.json` — campo `.version` é SemVer puro.
- **Flag de skip-permissions do agy**: `--dangerously-skip-permissions` (igual ao Claude). Confirmado via `agy help`.
- **IDEs Electron não respondem a `--version`** (Antigravity, Cursor, Windsurf). Para a versão real, ler `ProductVersion` do PE via `(Get-Item).VersionInfo.ProductVersion`.
- **DnD em Cards interativos**: aplicar `draggable` no wrapper que contém botões NÃO funciona porque os filhos consomem `mousedown`. Solução: handle dedicado isolado dos botões.
- **CliInfo struct serializa para o frontend**: novos campos opcionais precisam `#[serde(default)]` para não quebrar JSON salvo previamente.
- **`npm run build` NÃO roda tsc** — o script é só `vite build` (rolldown transpila TS mas não checa tipos com rigor). Para validar tipos (incl. `noUnusedLocals`/`noUnusedParameters` que estão `true` no tsconfig), rodar `npx tsc --noEmit` separadamente. `npm run test` = `vitest run`.
- **i18n type-safe**: `en.ts` importa `Dictionary` derivado de `pt-BR.ts`. Ao adicionar chave nova, adicionar em pt-BR.ts PRIMEIRO (fonte do tipo) e depois em en.ts, senão tsc quebra.
- **toastStore**: `import { showToast } from "../ui/toastStore"` — `showToast(message, variant)` com variant `success|error|warning|info`. Auto-dismiss em 4s.
- **ConfirmDialog** (`src/ui/ConfirmDialog.tsx`): modal de confirmação reutilizável; aceita `children` opcional renderizado entre `message` e as actions (ex: embutir um `SafeCommandPreview`). `variant="danger"` foca o botão Cancel no open. NÃO há handler global de Enter (botão focado dispara click nativo).
- **SafeCommandPreview** (`src/ui/SafeCommandPreview.tsx`): preview de comando com gating de risco. Prop `hideActions` esconde os botões próprios (para usar dentro de outro dialog que dona as actions); `onAckChange(boolean)` reporta o acknowledge do checkbox "dangerous" para o host gatar o confirm. Padrão de composição: `ConfirmDialog` + `SafeCommandPreview hideActions onAckChange` (usado em DoctorPage para o fix).
- **npm install exige `--legacy-peer-deps`**: `typescript@^6` conflita com o peerOptional `typescript@^5` de `i18next@24` (via react-i18next). `npm install <pkg>` falha com ERESOLVE; usar `npm install <pkg> --legacy-peer-deps`. Não há `.npmrc` no repo.
- **E2E (Playwright)**: `playwright.config.ts` tem `webServer.command="npm run dev"` na porta `5173` com `reuseExistingServer: !CI`. Rodar specs isolados sem dev server ativo dá timeout no `page.goto` (boot do Vite + 30s test timeout). Para iterar rápido: subir `npm run dev` em background, esperar HTTP 200 na 5173, depois `npx playwright test`. O diretório `e2e/` NÃO está no `tsconfig.json` (só `src`) — para typecheck do spec, usar tsconfig temporário ou rodar o próprio Playwright.
- **A11y test (axe-core)**: `@axe-core/playwright` instalado. O tema dark atual viola WCAG AA `color-contrast` em labels dimmed (~3.7:1, esperado 4.5:1). O teste de a11y desabilita a regra `color-contrast` (dívida de design, TODO) e ainda guarda regressões estruturais (labels/roles/aria/names). Tauri é stubado via `window.__TAURI_INTERNALS__` no `beforeEach`.
- **ChromeConnector (App.tsx)**: usa `useClis()` (não `useSyncExternalStore` cru) — o hook já subscreve E dispara `ensureLoaded`, garantindo que a StatusBar popule no boot. `useClis()` retorna `State & { refresh }`; `snapshot.clis`/`snapshot.checks` são os campos usados pela StatusBar.
- **Usage engine (config.rs)**: formatos REAIS de telemetria — Claude: `~/.claude/projects/**/*.jsonl` (recursivo, ~1000 arquivos); linhas `type:"assistant"` com `message.usage` {input_tokens, cache_creation_input_tokens, cache_read_input_tokens, output_tokens} e `message.model`; nome do dir pai = project. Codex: `~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl`; eventos `payload.type=="token_count"` com `payload.info.total_token_usage` CUMULATIVO (pegar o ULTIMO); modelo vem de linha `payload.type=="turn_context"` (`payload.model`); data do nome do arquivo `rollout-YYYY-MM-DD...`. Gemini NAO tem telemetria (CLI removido v15.2.0). tokens_in claude = input+cache_creation+cache_read; tokens_out codex = output+reasoning_output. Cache por path+mtime em `USAGE_CACHE: OnceLock<Mutex<HashMap>>`; `read_usage_stats(force: Option<bool>)` limpa o cache quando force=true.
- **Tauri command param opcional**: adicionar `force: Option<bool>` a um #[tauri::command] e backward-compatible — invoke sem o arg mapeia para None.
- **Session Engine (T3, session.rs)**: launch_cli/launch_custom_cli agora recebem `app: tauri::AppHandle` (param injetado pelo Tauri, NAO precisa passar do JS). Registro global de sessoes em `OnceLock<Mutex<HashMap<session_id, SessionEntry>>>` (lock com `unwrap_or_else(|p| p.into_inner())` p/ resiliencia a poison). Spawn DIRETO (pwsh/powershell/cmd) usa `tokio::process::Command` + `track_child` que faz `child.wait().await` numa task e emite evento Tauri `session-ended` {session_id, status, exit_code, duration_secs}. Caso `wt.exe`: o processo wt SAI na hora apos abrir a aba, entao NAO da p/ medir — `register_detached` emite `session-ended` com status="detached" imediatamente (nunca deixa "starting" eterno). Comandos `list_active_sessions`/`kill_session` (kill via `taskkill /PID x /T /F`). Frontend: `useSessionEvents()` montado 1x no App.tsx escuta `session-ended`, valida com zod, e chama markSessionEnded/updateSessionStatus (eram dead code). markSessionEnded agora aceita `durationMs` opcional (prefere duracao real do backend; fallback p/ wall-clock desde startedAt). Status "detached" adicionado ao type SessionStatus; UI (HistoryPage StatusBadge/timeline) trata via branch default (unknown).

- **Runbooks (B3, runbook.rs)**: comando `run_runbook_step(command, cwd: Option<String>, timeout_secs: Option<u64>) -> Result<StepResult,String>`. StepResult serializa snake_case (`exit_code`, `timed_out`) — o frontend mapeia `RawStepResult` (snake) -> camelCase. Executa via `powershell -NoProfile -NonInteractive -Command <safe>`, sanitiza com `sanitize_args` (rejeita metacaracteres, entao o comando NAO pode conter ; & | etc), valida cwd com `validate_directory` (vazio/None -> home). Timeout default 120s, max 1800s, via mpsc+recv_timeout+taskkill (mesmo padrao de run_silent_with_timeout). Step sem command = no-op success. invoke do JS usa chave `timeoutSecs` (camelCase) que Tauri mapeia para `timeout_secs`.
- **Stale closure em loops async no React**: usar `useRef` (cancelRef.current) para flag de cancelamento lido dentro de loop `for await`; um state `running` capturado pelo closure fica stale e quebra na 1a iteracao. Bug original do RunbookRunner: `if (!running) break` lia o valor antigo (false) logo apos setRunning(true).
- **RunbooksPanel.tsx**: monta list/edit/run inline na WorkspacePage via estado `showRunbooks`. RunbookEditor.onChange(null) = deletado (volta pra lista). cwd do runner = diretorio do workspace ativo (activeProfile?.directory). RunbooksCard agora recebe `onOpen` (nao mais onNavigate) — abre o painel em vez de navegar.

- **Perfis por projeto (B4, src/lib/projectProfile.ts)**: `.ailauncher.json` na raiz do projeto define `{version, cli?, provider?, env?, directory?, mcp?, runbook?}`. Schema zod tolerante (`.passthrough`); `parseProjectProfile(raw)` retorna `{ok,profile}|{ok:false,error}` (nunca lanca); `readProjectProfile(dir)` faz invoke do comando Rust `read_project_profile(directory)->Option<String>` (None=arquivo ausente, nao e erro). Comando Rust em commands/cli.rs: valida dir com `validate_directory`, junta nome FIXO `.ailauncher.json` (sem componente de path do caller -> sem path traversal), cap 256 KiB. **Merge de env (fix do "workspace decorativo")**: `mergeLaunchEnv(default, workspace, project)` com precedencia **projeto > workspace > default** (spread, sources tardios vencem; nunca muta). Ate B4 `WorkspaceProfile.envVars` era persistido mas NUNCA aplicado a nenhum launch. Agora LaunchDialog.doLaunch mescla provider-env (default) + `getActiveWorkspace(loadWorkspaces()).envVars` + `projectProfile.env`. Passa `null` ao backend quando merged vazio (preserva contrato: Rust so limpa ANTHROPIC_*/CLAUDE_* quando env_vars=Some). LaunchDialog le o profile em pickDirectory, recent-dir click, e no boot (lastDir); pre-preenche directory/provider (provider so se existir em providersState). Banner info quando profile.cli != cli aberto. MCP-por-projeto e schema-only em B4 (campo existe, nao cabeado). Comando custom Tauri NAO precisa entrada em capabilities/default.json (capabilities so controlam plugins/core; comandos do invoke_handler ja sao permitidos).

- **Smoke test do exe release**: o app usa `tauri-plugin-single-instance` — se a instância INSTALADA (`%LOCALAPPDATA%\AI Launcher\ai-launcher.exe`) estiver rodando, o exe novo de `target/release/` sai imediatamente (parece crash, mas é o plugin). Antes de smoke-testar um build novo: `Get-Process ai-launcher`, fechar a instância antiga (CloseMainWindow), só então lançar o novo. Validação saudável = processo vivo + MainWindowTitle preenchido + ~5 processos `msedgewebview2.exe` filhos.

- **axe color-contrast NAO isenta `aria-hidden`** — a regra avalia texto renderizado visualmente, fora da arvore de a11y ou nao. Para texto puramente decorativo (ex: watermark da StatusBar), usar `.exclude('.classe')` no AxeBuilder com comentario citando a excecao WCAG 1.4.3 "pure decoration". NUNCA re-desabilitar a regra inteira.
- **Teste de contraste em 2 camadas**: o teste de contrato (tokens hex vs superficies) NAO pega backgrounds compostos no DOM real (ex: --text-dim sobre item ativo accent-tinted do sidebar = 4.37). O axe e2e cobre o DOM; os dois sao complementares, manter ambos.
- **Inbox store (v16)**: eventos persistem CHAVES i18n (titleKey/params), nunca strings renderizadas — troca de locale re-renderiza certo. Dedup: conteudo identico preserva `read` (updates nao re-notificam a cada boot); `forceUnread: true` para transicoes do Doctor (conteudo identico por design mas DEVE re-notificar). bug-100.
- **Workflow morto por limite de sessao deixa trabalho PARCIAL na arvore** — o implementer da B1 morreu apos editar keys.ts/registry.ts (schema sem entry = erro tsc). Apos qualquer workflow falhar: `git status` + `git diff` antes de continuar; completar ou reverter o parcial conscientemente.

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->

- **[2026-07-07]** Nao confiar no auto-detect do `openwolf designqc` quando ha outros dev servers abertos; ele pode capturar outro projeto (ex: Nuxt em `localhost:3000`). Para o AI Launcher, subir/confirmar `http://127.0.0.1:5173` e passar `--url` explicitamente.
- **[2026-07-07]** Este projeto Tauri compila testes a partir de `src/main.rs` e nao expoe alvo de biblioteca; `cargo test --lib` falha com "no library targets found". Usar `cargo test <filtro>` ou `cargo test` no diretório `src-tauri`.
- **[2026-07-07]** Em redaction de backup, nao usar regex ampla `key` contra qualquer nome de propriedade; isso captura `cliKeys` e invalida schema/restores. Usar lista de nomes/campos secret-bearing e preservar metadados estruturais.
- **[2026-07-08]** Nao fazer polling agressivo da API publica do GitHub sem autenticacao; bate rate limit rapido ao monitorar Actions. Usar `gh auth status/login`, web UI, ou polling com backoff grande.
- **[2026-07-08]** Nao usar Playwright `page.screenshot` como primeira opcao para renderizar `docs/terminal-hero.svg` standalone; ele pode travar mesmo sem animacao. Usar `sharp` para PNG de conferência visual.

- **[2026-06-11]** ANTES de `npm run tauri build`: fechar qualquer `ai-launcher.exe` rodando (inclusive o de `target/release/` deixado por smoke test) — o cargo falha com "Acesso negado (os error 5)" ao substituir o binario em uso. E ao rodar build via pipe (`| tail`), SEMPRE checar `${PIPESTATUS[0]}` — o exit do tail (0) mascara a falha do build. bug-101.

- **[2026-05-19]** Adicionar CLI nova: SEMPRE validar o nome do binário e a existência do pacote npm rodando `where <cmd>` e `npm view <pkg> version` na máquina real antes de commitar. A entrada original do Antigravity ficou quebrada porque os campos foram preenchidos sem validação (`@google/antigravity` 404, `command: "antigravity"` inexistente).
- **[2026-05-19]** Para IDEs Electron, NUNCA confiar em `<exe> --version` — Electron mostra dialog/no-op. Ler `ProductVersion` direto do PE.
- **[2026-05-19]** Drag-and-drop dentro de `<Card>` com botões: NÃO aplicar `draggable` no wrapper externo. Usar handle dedicado isolado dos botões.
- **[2026-05-19]** HTML5 native drag (`draggable={true}` + `onDragStart`) tem comportamento inconsistente em Windows + WebView2 (Tauri). Mesmo com handle dedicado o gesto pode não disparar. **Solução padrão: usar `@dnd-kit` (pointer events) para qualquer DnD em apps Tauri.**
- **[2026-05-19]** Não confiar em CHANGELOG pré-existente. Quando há esboço de uma versão ainda não lançada, reescrever do zero com base no que realmente foi implementado.

- **[2026-06-10]** Reviewers de workflow (spec/quality) que avaliam só o RELATÓRIO TEXTUAL do implementer aprovam coisas que não estão na árvore. O Batch E de CSS foi "aprovado" mas as mudanças de tema nunca persistiram (provável perda por limite de sessão). SEMPRE rodar git diff/npm run build real e conferir os arquivos antes de commitar output de workflow — o finalReview que lê o diff real pegou; os reviewers por-batch que liam só o relatório não pegaram.

- **2026-06-10** — Beta2 escopo: MCP Manager CRUD completo (le/edita Claude ~/.claude/.mcp.json JSON, Codex ~/.codex/config.toml [mcp_servers.X] TOML, Gemini ~/.gemini/config/mcp_config.json JSON) + catalogo; Runbooks com execucao real (run_runbook_step) e UI cabeada; perfis por projeto (.ailauncher.json); Theme Foundry (contrato + teste anti-regressao + temas Phosphor/Midnight/HighContrast). Razao: usuario pediu as 4 frentes completas. MCP edita configs do usuario — backup obrigatorio antes de escrever.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->

- **2026-05-19** — Generalizar `CliInfo` com `extra_paths`+`update_manifest_url` em vez de hardcode por CLI. Razão: o droid já é script-installed; agy seria o segundo caso — abstrair foi mais barato que duplicar.
- **2026-05-19** — Ler `ProductVersion` via PowerShell em vez de crate `windows` nativa. Razão: 4 linhas de código vs nova dependência; tempo extra é aceitável (~200ms) e cacheável depois.
- **2026-05-19** — Drag handle `⋮⋮` visível em vez de drag invisível no wrapper. Razão: affordance + zero conflito de `mousedown` com botões filhos. UX fica óbvio para o usuário.
- **2026-05-19** — Update do IDE Antigravity adiado para v15.3. Razão: endpoint web (`antigravity-hub-auto-updater-...`) retorna 404 nos paths comuns; investigação requer captura de tráfego do app rodando. Versão local (ProductVersion) já mostra "2.0.0" — suficiente para v15.2.
- **2026-05-19** — Tasks do plano executadas inline com agrupamento (1+2, 4+5+6, 7-12) em vez de commit por task. Razão: dependências fortes entre tasks (clippy reclama de dead_code antes do consumidor existir). Granularidade do plano serve ao tracking, não à granularidade de commit.
- **2026-05-19** — Trocar HTML5 drag por `@dnd-kit` na v15.2.1. Razão: HTML5 native drag não disparava no ambiente Windows + WebView2 do usuário. `@dnd-kit` usa pointer events (universais), inclui suporte a teclado out-of-the-box e tem `activationConstraint` para não conflitar com cliques. +22 kB gzipped, vale a pena pela robustez.
