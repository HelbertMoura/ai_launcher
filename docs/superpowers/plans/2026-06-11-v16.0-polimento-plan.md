# v16.0 "Polimento" Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agent Analytics (evolução da CostsPage), Agent Inbox na TopBar e épico de acessibilidade AA completo, fechando com bump 16.0.0 + CHANGELOG.

**Architecture:** Analytics = funções puras sobre `UsageEntry[]` + componentes SVG próprios (zero dependência nova). Inbox = store persistido via `src/lib/storage` (registry zod) com dedup por chave estável, alimentado por 4 fontes existentes. AA = correção de tokens de contraste nos 7 temas + trava programática no theme-contract + ARIA combobox no palette + h1 por página.

**Tech Stack:** React 18 + TypeScript, zod, vitest (jsdom + @testing-library/react disponíveis), Playwright + axe-core, CSS vars (Theme Foundry).

**Execução (decisão do usuário):**
- Tarefas A1-A3 e B1-B3 via **workflow sequencial** — review de spec+qualidade por tarefa, **COMMIT IMEDIATO pós-aprovação dentro do workflow**, e validação do diff real por grep antes de cada commit (bug-057/059).
- Tarefas C1-C4 e D1-D2 **inline** (CSS de tema já se perdeu 2x em workflow).
- Validação por tarefa: `npx tsc --noEmit` + `npm test` no mínimo.

**Fatos verificados durante o planejamento (não re-descobrir):**
- O job e2e do Playwright **já existe** em `.github/workflows/quality.yml` (linhas 88-109) — "axe no CI" já está coberto; falta só reativar a regra `color-contrast` (Task C2).
- `--text-dim` reprova AA em 5 temas (dark 3.57, midnight 3.78, glacier 3.85, amber 4.08, light 4.11 — pior caso vs `--surface-2`); `--ok` do glacier (4.31) e `--warn` do light (4.42) também reprovam. Valores novos já calculados (Task C1).
- `Bell` já é exportado por `src/ui/icons.ts` (Phosphor icons).
- i18n fica em `src/i18n/locales/pt-BR.ts` (fonte do tipo `Dictionary`) e `en.ts` — SEMPRE adicionar chave em pt-BR primeiro.
- `npm install` exige `--legacy-peer-deps`.

---

## Frente A — Agent Analytics (workflow)

### Task A1: Módulo de agregação `analytics.ts`

**Files:**
- Create: `src/features/costs/analytics.ts`
- Test: `src/features/costs/analytics.test.ts`

- [ ] **Step 1: Write the failing tests**

```ts
// src/features/costs/analytics.test.ts
import { describe, expect, it } from "vitest";
import { byModel, byProject, dailySeries, trend } from "./analytics";
import type { UsageEntry } from "./useUsage";

function entry(partial: Partial<UsageEntry>): UsageEntry {
  return {
    date: "2026-06-10",
    cli: "claude",
    provider: "anthropic",
    model: "claude-opus",
    tokens_in: 100,
    tokens_out: 50,
    cost_estimate_usd: 1,
    project: "alpha",
    ...partial,
  };
}

describe("dailySeries", () => {
  it("returns one point per day, oldest first, zero-filled", () => {
    const entries = [
      entry({ date: "2026-06-10", cost_estimate_usd: 2 }),
      entry({ date: "2026-06-10", cost_estimate_usd: 3 }),
      entry({ date: "2026-06-08", cost_estimate_usd: 1 }),
    ];
    const series = dailySeries(entries, 3, "2026-06-10");
    expect(series).toHaveLength(3);
    expect(series[0]).toEqual({ date: "2026-06-08", tokensIn: 100, tokensOut: 50, costUsd: 1 });
    expect(series[1]).toEqual({ date: "2026-06-09", tokensIn: 0, tokensOut: 0, costUsd: 0 });
    expect(series[2].costUsd).toBe(5);
  });

  it("ignores entries outside the window", () => {
    const series = dailySeries([entry({ date: "2026-05-01" })], 3, "2026-06-10");
    expect(series.every((p) => p.costUsd === 0)).toBe(true);
  });

  it("handles empty input", () => {
    expect(dailySeries([], 30, "2026-06-10")).toHaveLength(30);
  });
});

describe("byProject", () => {
  it("ranks projects by cost desc with share, aggregating the rest as null label", () => {
    const entries = [
      entry({ project: "alpha", cost_estimate_usd: 6 }),
      entry({ project: "beta", cost_estimate_usd: 3 }),
      entry({ project: "gamma", cost_estimate_usd: 1 }),
    ];
    const rows = byProject(entries, 30, 2, "2026-06-10");
    expect(rows[0]).toEqual({ label: "alpha", costUsd: 6, share: 0.6 });
    expect(rows[1].label).toBe("beta");
    expect(rows[2]).toEqual({ label: null, costUsd: 1, share: 0.1 });
  });

  it("buckets null project as null label (renders as 'other')", () => {
    const rows = byProject([entry({ project: null, cost_estimate_usd: 2 })], 30, 8, "2026-06-10");
    expect(rows[0].label).toBeNull();
  });
});

describe("byModel", () => {
  it("ranks models by cost desc", () => {
    const entries = [
      entry({ model: "opus", cost_estimate_usd: 5 }),
      entry({ model: "haiku", cost_estimate_usd: 1 }),
      entry({ model: null, cost_estimate_usd: 2 }),
    ];
    const rows = byModel(entries, 30, "2026-06-10");
    expect(rows.map((r) => r.label)).toEqual(["opus", null, "haiku"]);
  });
});

describe("trend", () => {
  it("compares current window vs previous window", () => {
    const entries = [
      entry({ date: "2026-06-10", cost_estimate_usd: 4 }), // current (06-08..06-10)
      entry({ date: "2026-06-06", cost_estimate_usd: 2 }), // previous (06-05..06-07)
    ];
    const t = trend(entries, 3, "2026-06-10");
    expect(t.currentUsd).toBe(4);
    expect(t.previousUsd).toBe(2);
    expect(t.deltaPct).toBe(100);
  });

  it("returns null deltaPct when previous window is zero", () => {
    const t = trend([entry({ date: "2026-06-10" })], 3, "2026-06-10");
    expect(t.deltaPct).toBeNull();
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run src/features/costs/analytics.test.ts`
Expected: FAIL — `Cannot find module './analytics'`

- [ ] **Step 3: Implement `analytics.ts`**

```ts
// src/features/costs/analytics.ts
// Pure aggregation helpers for the Analytics view. No I/O, no Date.now —
// `today` is always injectable so every function is deterministic in tests.
import type { UsageEntry } from "./useUsage";

export interface DayPoint {
  date: string;
  tokensIn: number;
  tokensOut: number;
  costUsd: number;
}

export interface RankRow {
  /** null = aggregated bucket ("other" / unknown). */
  label: string | null;
  costUsd: number;
  /** Fraction of the window total, 0..1 (0 when total is 0). */
  share: number;
}

export interface Trend {
  currentUsd: number;
  previousUsd: number;
  /** Percent change vs previous window; null when previous is 0. */
  deltaPct: number | null;
}

function todayISO(): string {
  const d = new Date();
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`;
}

/** ISO date string N days before `iso` (N=0 returns iso). */
function shiftDays(iso: string, days: number): string {
  const d = new Date(`${iso}T00:00:00`);
  d.setDate(d.getDate() - days);
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`;
}

/** Continuous daily series for the last `days` days ending at `today`, zero-filled. */
export function dailySeries(entries: UsageEntry[], days = 30, today = todayISO()): DayPoint[] {
  const byDate = new Map<string, DayPoint>();
  for (let i = days - 1; i >= 0; i -= 1) {
    const date = shiftDays(today, i);
    byDate.set(date, { date, tokensIn: 0, tokensOut: 0, costUsd: 0 });
  }
  for (const e of entries) {
    const point = byDate.get(e.date);
    if (!point) continue; // outside window
    point.tokensIn += e.tokens_in;
    point.tokensOut += e.tokens_out;
    point.costUsd += e.cost_estimate_usd;
  }
  return [...byDate.values()];
}

function windowEntries(entries: UsageEntry[], days: number, today: string): UsageEntry[] {
  const start = shiftDays(today, days - 1);
  return entries.filter((e) => e.date >= start && e.date <= today);
}

function rank(
  entries: UsageEntry[],
  keyOf: (e: UsageEntry) => string | null,
  topN: number,
): RankRow[] {
  const acc = new Map<string | null, number>();
  let total = 0;
  for (const e of entries) {
    const key = keyOf(e);
    acc.set(key, (acc.get(key) ?? 0) + e.cost_estimate_usd);
    total += e.cost_estimate_usd;
  }
  const sorted = [...acc.entries()].sort((a, b) => b[1] - a[1]);
  const top = sorted.slice(0, topN);
  const restUsd = sorted.slice(topN).reduce((s, [, v]) => s + v, 0);
  const rows: RankRow[] = top.map(([label, costUsd]) => ({
    label,
    costUsd,
    share: total > 0 ? costUsd / total : 0,
  }));
  if (restUsd > 0) rows.push({ label: null, costUsd: restUsd, share: total > 0 ? restUsd / total : 0 });
  return rows;
}

export function byProject(entries: UsageEntry[], days = 30, topN = 8, today = todayISO()): RankRow[] {
  return rank(windowEntries(entries, days, today), (e) => e.project ?? null, topN);
}

export function byModel(entries: UsageEntry[], days = 30, today = todayISO()): RankRow[] {
  return rank(windowEntries(entries, days, today), (e) => e.model ?? null, 8);
}

export function trend(entries: UsageEntry[], days = 30, today = todayISO()): Trend {
  const currentStart = shiftDays(today, days - 1);
  const previousEnd = shiftDays(currentStart, 1);
  const previousStart = shiftDays(previousEnd, days - 1);
  let currentUsd = 0;
  let previousUsd = 0;
  for (const e of entries) {
    if (e.date >= currentStart && e.date <= today) currentUsd += e.cost_estimate_usd;
    else if (e.date >= previousStart && e.date <= previousEnd) previousUsd += e.cost_estimate_usd;
  }
  const deltaPct = previousUsd > 0 ? ((currentUsd - previousUsd) / previousUsd) * 100 : null;
  return { currentUsd, previousUsd, deltaPct };
}
```

> Nota sobre `byProject` no teste: com `topN = 2` e gamma agregado, o share de gamma é 1/10 = 0.1. O teste reflete isso.

- [ ] **Step 4: Run tests to verify they pass**

Run: `npx vitest run src/features/costs/analytics.test.ts`
Expected: PASS (8 testes)

- [ ] **Step 5: Validate types and commit**

```bash
npx tsc --noEmit
git add src/features/costs/analytics.ts src/features/costs/analytics.test.ts
git commit -m "feat(analytics): pure aggregation module (dailySeries, byProject, byModel, trend)"
```

### Task A2: Componentes de gráfico SVG (`src/ui/charts/`)

**Files:**
- Create: `src/ui/charts/geometry.ts`
- Create: `src/ui/charts/AreaChart.tsx`
- Create: `src/ui/charts/BarList.tsx`
- Create: `src/ui/charts/charts.css`
- Test: `src/ui/charts/geometry.test.ts`

- [ ] **Step 1: Write the failing geometry tests**

```ts
// src/ui/charts/geometry.test.ts
import { describe, expect, it } from "vitest";
import { buildAreaPath, buildLinePath, scalePoints } from "./geometry";

describe("scalePoints", () => {
  it("maps values to x evenly and y inverted (max at top)", () => {
    const pts = scalePoints([0, 5, 10], 100, 50, 0);
    expect(pts[0]).toEqual({ x: 0, y: 50 });
    expect(pts[2]).toEqual({ x: 100, y: 0 });
    expect(pts[1].y).toBe(25);
  });

  it("flat-lines at the bottom when all values are zero", () => {
    const pts = scalePoints([0, 0], 100, 50, 0);
    expect(pts.every((p) => p.y === 50)).toBe(true);
  });

  it("handles a single point (centered x)", () => {
    const pts = scalePoints([3], 100, 50, 0);
    expect(pts).toHaveLength(1);
    expect(pts[0].x).toBe(50);
  });
});

describe("paths", () => {
  it("buildLinePath produces M..L.. syntax", () => {
    const d = buildLinePath([{ x: 0, y: 10 }, { x: 5, y: 2 }]);
    expect(d).toBe("M 0 10 L 5 2");
  });

  it("buildAreaPath closes to the baseline", () => {
    const d = buildAreaPath([{ x: 0, y: 10 }, { x: 5, y: 2 }], 50);
    expect(d).toBe("M 0 10 L 5 2 L 5 50 L 0 50 Z");
  });

  it("returns empty string for no points", () => {
    expect(buildLinePath([])).toBe("");
    expect(buildAreaPath([], 50)).toBe("");
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run src/ui/charts/geometry.test.ts`
Expected: FAIL — `Cannot find module './geometry'`

- [ ] **Step 3: Implement geometry + components + CSS**

```ts
// src/ui/charts/geometry.ts
// Pure SVG path math for the hand-rolled charts. Numbers are rounded to 2
// decimals to keep snapshots/paths stable across platforms.
export interface XY {
  x: number;
  y: number;
}

function round2(n: number): number {
  return Math.round(n * 100) / 100;
}

/** Scale values into an SVG coordinate space. y is inverted (0 = top). */
export function scalePoints(values: number[], width: number, height: number, pad = 4): XY[] {
  if (values.length === 0) return [];
  const max = Math.max(...values);
  const innerH = height - pad * 2;
  if (values.length === 1) {
    const y = max > 0 ? pad : height - pad;
    return [{ x: round2(width / 2), y: round2(max > 0 ? y : height - pad) }];
  }
  const stepX = width / (values.length - 1);
  return values.map((v, i) => ({
    x: round2(i * stepX),
    y: round2(max > 0 ? pad + innerH * (1 - v / max) : height - pad),
  }));
}

export function buildLinePath(pts: XY[]): string {
  if (pts.length === 0) return "";
  return pts.map((p, i) => `${i === 0 ? "M" : "L"} ${p.x} ${p.y}`).join(" ");
}

export function buildAreaPath(pts: XY[], height: number): string {
  if (pts.length === 0) return "";
  const line = buildLinePath(pts);
  const last = pts[pts.length - 1];
  const first = pts[0];
  return `${line} L ${last.x} ${height} L ${first.x} ${height} Z`;
}
```

> Atenção ao caso `pad=0` e all-zero do teste: `scalePoints([0,0],100,50,0)` deve dar y=50 — a fórmula `height - pad` cobre isso. Para o caso single-point com valor >0 e pad=0, y=pad=0... o teste só verifica `x`; manter a implementação acima.

```tsx
// src/ui/charts/AreaChart.tsx
import { buildAreaPath, buildLinePath, scalePoints } from "./geometry";
import "./charts.css";

export interface AreaChartDatum {
  label: string;
  value: number;
}

interface AreaChartProps {
  data: AreaChartDatum[];
  /** Accessible description of the whole chart (required). */
  ariaLabel: string;
  height?: number;
  formatValue?: (v: number) => string;
}

const VIEW_W = 600;

export function AreaChart({ data, ariaLabel, height = 120, formatValue = String }: AreaChartProps) {
  const values = data.map((d) => d.value);
  const pts = scalePoints(values, VIEW_W, height);
  const first = data[0]?.label ?? "";
  const mid = data[Math.floor(data.length / 2)]?.label ?? "";
  const last = data[data.length - 1]?.label ?? "";

  return (
    <figure className="cd-chart" role="img" aria-label={ariaLabel}>
      <svg
        className="cd-chart__svg"
        viewBox={`0 0 ${VIEW_W} ${height}`}
        preserveAspectRatio="none"
        aria-hidden="true"
        focusable="false"
      >
        <path className="cd-chart__area" d={buildAreaPath(pts, height)} />
        <path className="cd-chart__line" d={buildLinePath(pts)} />
        {pts.map((p, i) => (
          <circle key={data[i].label} className="cd-chart__dot" cx={p.x} cy={p.y} r={3}>
            <title>{`${data[i].label}: ${formatValue(data[i].value)}`}</title>
          </circle>
        ))}
      </svg>
      <figcaption className="cd-chart__axis" aria-hidden="true">
        <span>{first}</span>
        <span>{mid}</span>
        <span>{last}</span>
      </figcaption>
    </figure>
  );
}
```

```tsx
// src/ui/charts/BarList.tsx
import "./charts.css";

export interface BarListItem {
  /** null = aggregated/unknown bucket; caller provides the display fallback. */
  label: string | null;
  value: number;
  /** 0..1 share of the total — drives the bar width. */
  share: number;
}

interface BarListProps {
  items: BarListItem[];
  ariaLabel: string;
  formatValue: (v: number) => string;
  /** Display text for null labels (e.g. t("costs.otherBucket")). */
  fallbackLabel: string;
}

export function BarList({ items, ariaLabel, formatValue, fallbackLabel }: BarListProps) {
  return (
    <ul className="cd-barlist" aria-label={ariaLabel}>
      {items.map((item, i) => (
        <li key={`${item.label ?? "__other"}-${i}`} className="cd-barlist__row">
          <span className="cd-barlist__label">{item.label ?? fallbackLabel}</span>
          <span className="cd-barlist__track" aria-hidden="true">
            <span
              className="cd-barlist__bar"
              style={{ width: `${Math.max(2, Math.round(item.share * 100))}%` }}
            />
          </span>
          <span className="cd-barlist__value">{formatValue(item.value)}</span>
        </li>
      ))}
    </ul>
  );
}
```

```css
/* src/ui/charts/charts.css — themed exclusively via Theme Foundry vars */
.cd-chart {
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.cd-chart__svg {
  width: 100%;
  height: auto;
  display: block;
}

.cd-chart__area {
  fill: var(--accent-soft);
  stroke: none;
}

.cd-chart__line {
  fill: none;
  stroke: var(--accent);
  stroke-width: 2;
  vector-effect: non-scaling-stroke;
}

.cd-chart__dot {
  fill: var(--accent);
}

.cd-chart__axis {
  display: flex;
  justify-content: space-between;
  font-size: 11px;
  color: var(--text-dim);
}

.cd-barlist {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.cd-barlist__row {
  display: grid;
  grid-template-columns: minmax(80px, 1fr) 2fr auto;
  align-items: center;
  gap: 10px;
}

.cd-barlist__label {
  font-size: 12px;
  color: var(--text-muted);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.cd-barlist__track {
  display: block;
  height: 8px;
  border-radius: 4px;
  background: var(--surface-2);
  overflow: hidden;
}

.cd-barlist__bar {
  display: block;
  height: 100%;
  border-radius: 4px;
  background: var(--accent);
}

.cd-barlist__value {
  font-size: 12px;
  color: var(--text);
  font-variant-numeric: tabular-nums;
}
```

- [ ] **Step 4: Run tests, typecheck**

Run: `npx vitest run src/ui/charts/geometry.test.ts && npx tsc --noEmit`
Expected: PASS / exit 0

- [ ] **Step 5: Commit**

```bash
git add src/ui/charts/
git commit -m "feat(charts): hand-rolled SVG AreaChart + BarList themed via Theme Foundry vars"
```

### Task A3: Integração na CostsPage + i18n "Analytics"

**Files:**
- Modify: `src/features/costs/CostsPage.tsx`
- Modify: `src/features/costs/CostsPage.css` (acrescentar classes da seção analytics)
- Modify: `src/i18n/locales/pt-BR.ts` (PRIMEIRO — fonte do tipo)
- Modify: `src/i18n/locales/en.ts`

- [ ] **Step 1: Adicionar chaves i18n**

Em `src/i18n/locales/pt-BR.ts`, no bloco `tabs:` trocar `costs: "Custos"` por `costs: "Analytics"`. No bloco `costs:` (linha ~228) adicionar:

```ts
    seriesTitle: "Custo por dia (30d)",
    topProjects: "Top projetos (30d)",
    byModelTitle: "Por modelo (30d)",
    cost30d: "Custo (30d)",
    tokens30d: "Tokens (30d)",
    trendVsPrev: "{{delta}} vs período anterior",
    trendNew: "sem período anterior para comparar",
    otherBucket: "outros",
```

Em `en.ts` (mesmas chaves): `costs: "Analytics"` em tabs; e no bloco costs:

```ts
    seriesTitle: "Cost per day (30d)",
    topProjects: "Top projects (30d)",
    byModelTitle: "By model (30d)",
    cost30d: "Cost (30d)",
    tokens30d: "Tokens (30d)",
    trendVsPrev: "{{delta}} vs previous period",
    trendNew: "no previous period to compare",
    otherBucket: "other",
```

- [ ] **Step 2: Integrar na CostsPage**

Em `src/features/costs/CostsPage.tsx`:

1. Imports novos no topo:

```tsx
import { AreaChart } from "../../ui/charts/AreaChart";
import { BarList } from "../../ui/charts/BarList";
import { byModel, byProject, dailySeries, trend } from "./analytics";
```

2. Dentro do componente, após o `useMemo` existente de `{ todayTotal, cliRollups }`, adicionar:

```tsx
  const analytics = useMemo(() => {
    const entries = report?.entries ?? [];
    return {
      series: dailySeries(entries, 30),
      projects: byProject(entries, 30, 8),
      models: byModel(entries, 30),
      trend30: trend(entries, 30),
    };
  }, [report]);

  const trendLabel = useMemo(() => {
    const { deltaPct } = analytics.trend30;
    if (deltaPct === null) return t("costs.trendNew");
    const signed = `${deltaPct >= 0 ? "+" : ""}${deltaPct.toFixed(0)}%`;
    return t("costs.trendVsPrev", { delta: signed });
  }, [analytics.trend30, t]);

  const tokens30d = useMemo(
    () => analytics.series.reduce((s, p) => s + p.tokensIn + p.tokensOut, 0),
    [analytics.series],
  );
```

3. No JSX, dentro do bloco `{!loading && hasData && (<> ... </>)}`, logo APÓS o `<Card className="cd-costs__hero">...</Card>` e ANTES do bloco `{cliRollups.length > 0 && (...)}`, inserir:

```tsx
          <div className="cd-page__grid cd-costs__analytics-cards">
            <Card className="cd-costs__stat">
              <div className="cd-costs__stat-label">{t("costs.cost30d")}</div>
              <div className="cd-costs__stat-value">{formatUsd(analytics.trend30.currentUsd)}</div>
              <div className="cd-costs__stat-sub">{trendLabel}</div>
            </Card>
            <Card className="cd-costs__stat">
              <div className="cd-costs__stat-label">{t("costs.tokens30d")}</div>
              <div className="cd-costs__stat-value">{tokens30d.toLocaleString()}</div>
            </Card>
          </div>

          <Card className="cd-costs__chart-card">
            <h3 className="cd-costs__section">{t("costs.seriesTitle")}</h3>
            <AreaChart
              data={analytics.series.map((p) => ({ label: p.date.slice(5), value: p.costUsd }))}
              ariaLabel={t("costs.seriesTitle")}
              formatValue={formatUsd}
            />
          </Card>

          <div className="cd-page__grid cd-costs__rankings">
            <Card>
              <h3 className="cd-costs__section">{t("costs.topProjects")}</h3>
              <BarList
                items={analytics.projects}
                ariaLabel={t("costs.topProjects")}
                formatValue={formatUsd}
                fallbackLabel={t("costs.otherBucket")}
              />
            </Card>
            <Card>
              <h3 className="cd-costs__section">{t("costs.byModelTitle")}</h3>
              <BarList
                items={analytics.models}
                ariaLabel={t("costs.byModelTitle")}
                formatValue={formatUsd}
                fallbackLabel={t("costs.otherBucket")}
              />
            </Card>
          </div>
```

> NOTA: na Task C4 os `<h3 className="cd-costs__section">` desta página serão promovidos a `<h2>`. Se C4 já tiver rodado, usar `<h2>` direto aqui.

4. Em `src/features/costs/CostsPage.css`, acrescentar ao final:

```css
.cd-costs__analytics-cards {
  margin-top: 16px;
}

.cd-costs__stat-label {
  font-size: 12px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

.cd-costs__stat-value {
  font-size: 24px;
  font-weight: 600;
  color: var(--text);
  font-variant-numeric: tabular-nums;
}

.cd-costs__stat-sub {
  font-size: 12px;
  color: var(--text-dim);
}

.cd-costs__chart-card {
  margin-top: 16px;
}

.cd-costs__rankings {
  margin-top: 16px;
}
```

- [ ] **Step 3: Validar**

Run: `npx tsc --noEmit && npm test && npm run build`
Expected: tudo verde. Abrir `npm run dev` é opcional para inspeção visual.

- [ ] **Step 4: Commit**

```bash
git add src/features/costs/ src/i18n/locales/ src/ui/charts/
git commit -m "feat(analytics): CostsPage evolves into Analytics — 30d series, project/model rankings, trend"
```

---

## Frente B — Agent Inbox (workflow)

### Task B1: Store do Inbox + registro de persistência

**Files:**
- Modify: `src/lib/storage/keys.ts` (nova key)
- Modify: `src/lib/storage/registry.ts` (nova entry)
- Create: `src/features/inbox/inboxStore.ts`
- Test: `src/features/inbox/inboxStore.test.ts`

- [ ] **Step 1: Registrar a key**

Em `src/lib/storage/keys.ts`, adicionar dentro de `STORAGE_KEYS` (seção "v15" ou nova seção v16):

```ts
  // --- Inbox (v16) ---
  inbox: 'ai-launcher:v16:inbox',
```

Em `src/lib/storage/registry.ts`, adicionar schemas perto dos leaf schemas:

```ts
const inboxEventSchema = z
  .object({
    id: z.string().min(1),
    type: z.enum(['session', 'budget', 'update', 'doctor']),
    titleKey: z.string().min(1),
    titleParams: z.record(z.string(), z.union([z.string(), z.number()])).optional(),
    bodyKey: z.string().optional(),
    bodyParams: z.record(z.string(), z.union([z.string(), z.number()])).optional(),
    ts: z.number(),
    read: z.boolean(),
    targetTab: z.string(),
  })
  .passthrough();

const inboxStateSchema = z
  .object({
    events: z.array(inboxEventSchema).default([]),
    doctorFailing: z.array(z.string()).default([]),
  })
  .passthrough();
```

E a entry no REGISTRY (após `budget`):

```ts
  inbox: entry({
    id: 'inbox',
    key: STORAGE_KEYS.inbox,
    schema: inboxStateSchema,
    default: { events: [], doctorFailing: [] } as z.infer<typeof inboxStateSchema>,
    version: 1,
  }),
```

- [ ] **Step 2: Write the failing store tests**

```ts
// src/features/inbox/inboxStore.test.ts
import { beforeEach, describe, expect, it } from "vitest";
import {
  clearAll,
  getInboxSnapshot,
  markAllRead,
  markRead,
  pushEvent,
  reportDoctorResults,
  unreadCount,
  __resetForTests,
} from "./inboxStore";

function evt(id: string, extra: Record<string, unknown> = {}) {
  return {
    id,
    type: "update" as const,
    titleKey: "inbox.updateTitle",
    targetTab: "updates" as const,
    ...extra,
  };
}

describe("inboxStore", () => {
  beforeEach(() => {
    localStorage.clear();
    __resetForTests();
  });

  it("pushEvent adds an unread event with timestamp", () => {
    pushEvent(evt("update:claude:2.0.0"));
    const snap = getInboxSnapshot();
    expect(snap.events).toHaveLength(1);
    expect(snap.events[0].read).toBe(false);
    expect(unreadCount()).toBe(1);
  });

  it("dedups by id: re-push updates ts and re-marks unread, never duplicates", () => {
    pushEvent(evt("update:claude:2.0.0"));
    markRead("update:claude:2.0.0");
    pushEvent(evt("update:claude:2.0.0"));
    const snap = getInboxSnapshot();
    expect(snap.events).toHaveLength(1);
    expect(snap.events[0].read).toBe(false);
  });

  it("caps at 50 events, evicting oldest read first", () => {
    for (let i = 0; i < 50; i += 1) pushEvent(evt(`e${i}`));
    markRead("e0");
    pushEvent(evt("e50"));
    const snap = getInboxSnapshot();
    expect(snap.events).toHaveLength(50);
    expect(snap.events.find((e) => e.id === "e0")).toBeUndefined();
    expect(snap.events.find((e) => e.id === "e50")).toBeDefined();
  });

  it("caps at 50 evicting oldest overall when none are read", () => {
    for (let i = 0; i < 51; i += 1) pushEvent(evt(`e${i}`));
    const snap = getInboxSnapshot();
    expect(snap.events).toHaveLength(50);
    expect(snap.events.find((e) => e.id === "e0")).toBeUndefined();
  });

  it("markAllRead and clearAll work", () => {
    pushEvent(evt("a"));
    pushEvent(evt("b"));
    markAllRead();
    expect(unreadCount()).toBe(0);
    clearAll();
    expect(getInboxSnapshot().events).toHaveLength(0);
  });

  it("reportDoctorResults only notifies on ok->fail transition", () => {
    reportDoctorResults([
      { key: "node", name: "Node.js", installed: false },
      { key: "git", name: "Git", installed: true },
    ]);
    expect(getInboxSnapshot().events).toHaveLength(1);
    // Same failure reported again: no new event, no re-unread
    markAllRead();
    reportDoctorResults([{ key: "node", name: "Node.js", installed: false }]);
    expect(unreadCount()).toBe(0);
    // Recovers, then fails again: new notification
    reportDoctorResults([{ key: "node", name: "Node.js", installed: true }]);
    reportDoctorResults([{ key: "node", name: "Node.js", installed: false }]);
    expect(unreadCount()).toBe(1);
  });

  it("persists through storage and survives reload (new module read)", () => {
    pushEvent(evt("persisted"));
    __resetForTests(); // drops in-memory cache, forces re-read from localStorage
    expect(getInboxSnapshot().events.find((e) => e.id === "persisted")).toBeDefined();
  });
});
```

- [ ] **Step 3: Run tests to verify they fail**

Run: `npx vitest run src/features/inbox/inboxStore.test.ts`
Expected: FAIL — módulo inexistente

- [ ] **Step 4: Implement `inboxStore.ts`**

```ts
// src/features/inbox/inboxStore.ts
// Persisted notification inbox. Events carry i18n KEYS (not rendered strings)
// so locale switches re-render correctly. Dedup is by stable `id`: re-pushing
// an existing id refreshes ts + unread instead of duplicating.
import { useSyncExternalStore } from "react";
import { readKey, writeKey } from "../../lib/storage";
import type { TabId } from "../../app/layout/TabId";

export type InboxType = "session" | "budget" | "update" | "doctor";

export interface InboxEvent {
  id: string;
  type: InboxType;
  titleKey: string;
  titleParams?: Record<string, string | number>;
  bodyKey?: string;
  bodyParams?: Record<string, string | number>;
  ts: number;
  read: boolean;
  targetTab: TabId;
}

export interface InboxState {
  events: InboxEvent[];
  /** Doctor check keys currently failing — drives ok->fail transition detection. */
  doctorFailing: string[];
}

const MAX_EVENTS = 50;

let state: InboxState | null = null;
const listeners = new Set<() => void>();

function load(): InboxState {
  if (state === null) {
    const raw = readKey("inbox");
    state = {
      events: (raw.events as InboxEvent[]) ?? [],
      doctorFailing: raw.doctorFailing ?? [],
    };
  }
  return state;
}

function save(next: InboxState): void {
  state = next;
  writeKey("inbox", next as never);
  for (const fn of listeners) fn();
}

export function getInboxSnapshot(): InboxState {
  return load();
}

export function subscribe(fn: () => void): () => void {
  listeners.add(fn);
  return () => listeners.delete(fn);
}

export function unreadCount(): number {
  return load().events.filter((e) => !e.read).length;
}

/** Evict beyond MAX_EVENTS: oldest read first, then oldest overall. */
function evict(events: InboxEvent[]): InboxEvent[] {
  if (events.length <= MAX_EVENTS) return events;
  const sorted = [...events].sort((a, b) => a.ts - b.ts);
  let result = [...events];
  while (result.length > MAX_EVENTS) {
    const victim = sorted.find((e) => e.read && result.includes(e)) ?? sorted.find((e) => result.includes(e));
    if (!victim) break;
    result = result.filter((e) => e !== victim);
  }
  return result;
}

export type PushInput = Omit<InboxEvent, "ts" | "read"> & { ts?: number };

export function pushEvent(input: PushInput): void {
  const cur = load();
  const ts = input.ts ?? Date.now();
  const existing = cur.events.find((e) => e.id === input.id);
  let events: InboxEvent[];
  if (existing) {
    events = cur.events.map((e) =>
      e.id === input.id ? { ...e, ...input, ts, read: false } : e,
    );
  } else {
    events = evict([...cur.events, { ...input, ts, read: false }]);
  }
  save({ ...cur, events });
}

export function markRead(id: string): void {
  const cur = load();
  save({ ...cur, events: cur.events.map((e) => (e.id === id ? { ...e, read: true } : e)) });
}

export function markAllRead(): void {
  const cur = load();
  save({ ...cur, events: cur.events.map((e) => ({ ...e, read: true })) });
}

export function clearAll(): void {
  const cur = load();
  save({ ...cur, events: [] });
}

export interface DoctorResult {
  key: string;
  name: string;
  installed: boolean;
}

/** Push doctor events ONLY for ok->fail transitions (anti-spam). */
export function reportDoctorResults(results: DoctorResult[]): void {
  const cur = load();
  const previouslyFailing = new Set(cur.doctorFailing);
  const nowFailing = results.filter((r) => !r.installed).map((r) => r.key);
  const newlyFailing = results.filter((r) => !r.installed && !previouslyFailing.has(r.key));

  // Keys not present in this run keep their previous failing status.
  const reportedKeys = new Set(results.map((r) => r.key));
  const carried = cur.doctorFailing.filter((k) => !reportedKeys.has(k));
  const nextFailing = [...new Set([...carried, ...nowFailing])];

  let next: InboxState = { ...cur, doctorFailing: nextFailing };
  state = next;
  for (const r of newlyFailing) {
    // pushEvent persists + notifies; update local ref afterwards.
    pushEvent({
      id: `doctor:${r.key}`,
      type: "doctor",
      titleKey: "inbox.doctorTitle",
      titleParams: { name: r.name },
      targetTab: "doctor",
    });
    next = load();
  }
  if (newlyFailing.length === 0) {
    save(next);
  }
}

/** Test-only: drop the in-memory cache so the next read hits localStorage. */
export function __resetForTests(): void {
  state = null;
}

/** React hook — re-renders on every store mutation. */
export function useInbox(): InboxState {
  return useSyncExternalStore(subscribe, getInboxSnapshot);
}
```

> CUIDADO com `useSyncExternalStore`: `getInboxSnapshot` precisa retornar a MESMA referência entre mutações (o `save()` substitui `state`, então ok). Nunca retornar objeto novo a cada chamada.
> CUIDADO no `reportDoctorResults`: garantir que `doctorFailing` é persistido mesmo quando há pushes (o `pushEvent` salva `state` que já contém `doctorFailing` atualizado porque mutamos `state` antes do loop).

- [ ] **Step 5: Run tests to verify they pass**

Run: `npx vitest run src/features/inbox/inboxStore.test.ts && npx tsc --noEmit`
Expected: PASS / exit 0

- [ ] **Step 6: Commit**

```bash
git add src/lib/storage/keys.ts src/lib/storage/registry.ts src/features/inbox/
git commit -m "feat(inbox): persisted inbox store with stable-id dedup, 50-event cap, doctor transition tracking"
```

### Task B2: UI — sino + painel na TopBar

**Files:**
- Create: `src/features/inbox/InboxBell.tsx`
- Create: `src/features/inbox/InboxBell.css`
- Modify: `src/app/layout/TopBar.tsx` (prop `onNavigate` + render do sino)
- Modify: `src/app/App.tsx` (passar `onNavigate={onSelect}` na TopBar via ChromeConnector)
- Modify: `src/i18n/locales/pt-BR.ts` e `en.ts` (bloco `inbox`)

- [ ] **Step 1: i18n (pt-BR PRIMEIRO, depois en)**

pt-BR.ts — novo bloco top-level `inbox:`:

```ts
  inbox: {
    title: "Inbox",
    open: "Abrir inbox ({{count}} não lidas)",
    openEmpty: "Abrir inbox",
    empty: "Nenhuma notificação",
    markAllRead: "Marcar tudo como lido",
    clearAll: "Limpar",
    sessionCompleted: "Sessão de {{cli}} concluída",
    sessionFailed: "Sessão de {{cli}} falhou",
    sessionDetached: "Sessão de {{cli}} aberta no terminal",
    sessionDuration: "duração: {{duration}}",
    budgetTitle: "Budget de {{provider}}",
    budgetWarning: "{{percent}}% do limite usado",
    budgetExceeded: "limite excedido ({{percent}}%)",
    updateTitle: "Update disponível: {{cli}} {{version}}",
    doctorTitle: "Doctor: {{name}} com problema",
  },
```

en.ts — mesmas chaves:

```ts
  inbox: {
    title: "Inbox",
    open: "Open inbox ({{count}} unread)",
    openEmpty: "Open inbox",
    empty: "No notifications",
    markAllRead: "Mark all as read",
    clearAll: "Clear",
    sessionCompleted: "{{cli}} session completed",
    sessionFailed: "{{cli}} session failed",
    sessionDetached: "{{cli}} session opened in terminal",
    sessionDuration: "duration: {{duration}}",
    budgetTitle: "{{provider}} budget",
    budgetWarning: "{{percent}}% of limit used",
    budgetExceeded: "limit exceeded ({{percent}}%)",
    updateTitle: "Update available: {{cli}} {{version}}",
    doctorTitle: "Doctor: {{name}} has a problem",
  },
```

- [ ] **Step 2: Implement `InboxBell.tsx`**

```tsx
// src/features/inbox/InboxBell.tsx
import { useCallback, useEffect, useRef, useState } from "react";
import { useTranslation } from "react-i18next";
import { Bell } from "../../ui/icons";
import type { TabId } from "../../app/layout/TabId";
import {
  clearAll,
  markAllRead,
  markRead,
  useInbox,
  type InboxEvent,
} from "./inboxStore";
import "./InboxBell.css";

const TYPE_GLYPH: Record<InboxEvent["type"], string> = {
  session: "⟲",
  budget: "$",
  update: "↑",
  doctor: "✚",
};

function relativeTime(ts: number): string {
  const diff = Date.now() - ts;
  if (diff < 60_000) return "agora";
  const min = Math.floor(diff / 60_000);
  if (min < 60) return `${min}m`;
  const h = Math.floor(min / 60);
  if (h < 24) return `${h}h`;
  return `${Math.floor(h / 24)}d`;
}

interface InboxBellProps {
  onNavigate: (tab: TabId) => void;
}

export function InboxBell({ onNavigate }: InboxBellProps) {
  const { t } = useTranslation();
  const { events } = useInbox();
  const [open, setOpen] = useState(false);
  const rootRef = useRef<HTMLDivElement>(null);
  const buttonRef = useRef<HTMLButtonElement>(null);
  const itemRefs = useRef<Array<HTMLButtonElement | null>>([]);

  const unread = events.filter((e) => !e.read).length;
  const sorted = [...events].sort((a, b) => b.ts - a.ts);

  const closeAndRestoreFocus = useCallback(() => {
    setOpen(false);
    buttonRef.current?.focus();
  }, []);

  // Click-outside closes the panel.
  useEffect(() => {
    if (!open) return;
    const onDocClick = (e: MouseEvent) => {
      if (rootRef.current && !rootRef.current.contains(e.target as Node)) setOpen(false);
    };
    document.addEventListener("mousedown", onDocClick);
    return () => document.removeEventListener("mousedown", onDocClick);
  }, [open]);

  const handlePanelKeyDown = (e: React.KeyboardEvent<HTMLDivElement>): void => {
    if (e.key === "Escape") {
      e.preventDefault();
      closeAndRestoreFocus();
      return;
    }
    if (e.key === "ArrowDown" || e.key === "ArrowUp") {
      e.preventDefault();
      const items = itemRefs.current.filter(Boolean) as HTMLButtonElement[];
      if (items.length === 0) return;
      const idx = items.findIndex((el) => el === document.activeElement);
      const next =
        e.key === "ArrowDown"
          ? items[(idx + 1) % items.length]
          : items[(idx - 1 + items.length) % items.length];
      next.focus();
    }
  };

  const handleItemClick = (evt: InboxEvent): void => {
    markRead(evt.id);
    setOpen(false);
    onNavigate(evt.targetTab);
  };

  itemRefs.current = new Array(sorted.length).fill(null);

  return (
    <div className="cd-inbox" ref={rootRef}>
      <button
        ref={buttonRef}
        type="button"
        className="cd-inbox__bell"
        aria-haspopup="true"
        aria-expanded={open}
        aria-label={unread > 0 ? t("inbox.open", { count: unread }) : t("inbox.openEmpty")}
        onClick={() => setOpen((o) => !o)}
      >
        <Bell size={16} aria-hidden />
        {unread > 0 && (
          <span className="cd-inbox__badge" aria-hidden>
            {unread > 9 ? "9+" : unread}
          </span>
        )}
      </button>

      {open && (
        <div
          className="cd-inbox__panel"
          role="dialog"
          aria-label={t("inbox.title")}
          onKeyDown={handlePanelKeyDown}
        >
          <div className="cd-inbox__head">
            <span className="cd-inbox__head-title">{t("inbox.title")}</span>
          </div>
          <div className="cd-inbox__list">
            {sorted.length === 0 ? (
              <div className="cd-inbox__empty">{t("inbox.empty")}</div>
            ) : (
              sorted.map((evt, i) => (
                <button
                  key={evt.id}
                  ref={(el) => {
                    itemRefs.current[i] = el;
                  }}
                  type="button"
                  className={`cd-inbox__item${evt.read ? "" : " cd-inbox__item--unread"}`}
                  onClick={() => handleItemClick(evt)}
                >
                  <span className="cd-inbox__item-icon" aria-hidden>
                    {TYPE_GLYPH[evt.type]}
                  </span>
                  <span className="cd-inbox__item-body">
                    <span className="cd-inbox__item-title">
                      {t(evt.titleKey, evt.titleParams)}
                    </span>
                    {evt.bodyKey && (
                      <span className="cd-inbox__item-desc">{t(evt.bodyKey, evt.bodyParams)}</span>
                    )}
                  </span>
                  <span className="cd-inbox__item-time">{relativeTime(evt.ts)}</span>
                </button>
              ))
            )}
          </div>
          {sorted.length > 0 && (
            <div className="cd-inbox__foot">
              <button type="button" className="cd-inbox__foot-btn" onClick={() => markAllRead()}>
                {t("inbox.markAllRead")}
              </button>
              <button type="button" className="cd-inbox__foot-btn" onClick={() => clearAll()}>
                {t("inbox.clearAll")}
              </button>
            </div>
          )}
        </div>
      )}
    </div>
  );
}
```

> `t(evt.titleKey, evt.titleParams)`: o i18next aceita `t(key, params)`; tipar como `t(evt.titleKey as never, evt.titleParams as never)` SE o tsc reclamar de chave dinâmica (o projeto usa Dictionary tipado). Preferir um cast localizado a afrouxar o tipo global.

- [ ] **Step 3: CSS `InboxBell.css`**

```css
/* src/features/inbox/InboxBell.css */
.cd-inbox {
  position: relative;
}

.cd-inbox__bell {
  position: relative;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 30px;
  height: 30px;
  border: 1px solid var(--border);
  border-radius: 6px;
  background: transparent;
  color: var(--text-muted);
  cursor: pointer;
}

.cd-inbox__bell:hover,
.cd-inbox__bell:focus-visible {
  color: var(--text);
  border-color: var(--border-strong);
}

.cd-inbox__badge {
  position: absolute;
  top: -5px;
  right: -5px;
  min-width: 15px;
  height: 15px;
  padding: 0 3px;
  border-radius: 8px;
  background: var(--accent);
  color: var(--accent-ink);
  font-size: 10px;
  font-weight: 700;
  line-height: 15px;
  text-align: center;
}

.cd-inbox__panel {
  position: absolute;
  top: calc(100% + 8px);
  right: 0;
  width: 320px;
  max-height: 420px;
  display: flex;
  flex-direction: column;
  background: var(--surface-1);
  border: 1px solid var(--border-strong);
  border-radius: 10px;
  box-shadow: 0 12px 32px rgb(0 0 0 / 0.35);
  z-index: 60;
  overflow: hidden;
}

.cd-inbox__head {
  padding: 10px 12px;
  border-bottom: 1px solid var(--border);
}

.cd-inbox__head-title {
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-muted);
}

.cd-inbox__list {
  overflow-y: auto;
  flex: 1;
}

.cd-inbox__empty {
  padding: 24px 12px;
  text-align: center;
  font-size: 12px;
  color: var(--text-dim);
}

.cd-inbox__item {
  display: grid;
  grid-template-columns: auto 1fr auto;
  gap: 10px;
  align-items: start;
  width: 100%;
  padding: 10px 12px;
  border: none;
  border-bottom: 1px solid var(--border);
  background: transparent;
  text-align: left;
  cursor: pointer;
  color: var(--text-muted);
}

.cd-inbox__item:hover,
.cd-inbox__item:focus-visible {
  background: var(--surface-hover);
}

.cd-inbox__item--unread {
  color: var(--text);
  background: var(--surface-2);
}

.cd-inbox__item-icon {
  color: var(--accent);
  font-size: 13px;
}

.cd-inbox__item-body {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}

.cd-inbox__item-title {
  font-size: 13px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.cd-inbox__item-desc {
  font-size: 11px;
  color: var(--text-dim);
}

.cd-inbox__item-time {
  font-size: 11px;
  color: var(--text-dim);
  font-variant-numeric: tabular-nums;
}

.cd-inbox__foot {
  display: flex;
  gap: 8px;
  padding: 8px 12px;
  border-top: 1px solid var(--border);
}

.cd-inbox__foot-btn {
  flex: 1;
  padding: 5px 8px;
  font-size: 11px;
  border: 1px solid var(--border);
  border-radius: 6px;
  background: transparent;
  color: var(--text-muted);
  cursor: pointer;
}

.cd-inbox__foot-btn:hover,
.cd-inbox__foot-btn:focus-visible {
  color: var(--text);
  border-color: var(--border-strong);
}
```

- [ ] **Step 4: Wire na TopBar e App**

`src/app/layout/TopBar.tsx`:

1. Import: `import { InboxBell } from "../../features/inbox/InboxBell";` e `import type { TabId } from "./TabId";`
2. Prop nova em `TopBarProps`: `onNavigate: (tab: TabId) => void;`
3. Desestruturar `onNavigate` nos params.
4. No JSX, dentro de `<div className="cd-top__right">`, ANTES do bloco `cd-top__accents`, inserir:

```tsx
        <InboxBell onNavigate={onNavigate} />
```

`src/app/App.tsx` (ChromeConnector): no `<TopBar ...>` adicionar `onNavigate={onSelect}`.

- [ ] **Step 5: Validar e commitar**

Run: `npx tsc --noEmit && npm test && npm run build`
Expected: verde.

```bash
git add src/features/inbox/ src/app/layout/TopBar.tsx src/app/App.tsx src/i18n/locales/
git commit -m "feat(inbox): bell + accessible dropdown panel in TopBar with unread badge"
```

### Task B3: Fontes de eventos (sessões, budget, updates, doctor)

**Files:**
- Modify: `src/features/history/useSessionEvents.ts`
- Modify: `src/app/App.tsx` (efeito de budget existente)
- Modify: `src/app/App.tsx` (ChromeConnector — efeito de updates)
- Modify: `src/features/workspace/DoctorPage.tsx`
- Test: `src/features/history/useSessionEvents.test.ts` (estender)

- [ ] **Step 1: Sessões — em `useSessionEvents.ts`**

Adicionar import e push dentro de `applySessionEnded` (no final da função, após o tratamento de history):

```ts
import { pushEvent } from "../inbox/inboxStore";

// ... dentro de applySessionEnded, ao final:
function sessionCli(sessionId: string): string {
  try {
    const raw = localStorage.getItem("ai-launcher-config");
    if (!raw) return "CLI";
    const cfg = JSON.parse(raw) as { history?: Array<{ sessionId?: string; cli?: string; cliKey?: string }> };
    const item = cfg.history?.find((h) => h.sessionId === sessionId);
    return item?.cli || item?.cliKey || "CLI";
  } catch {
    return "CLI";
  }
}
```

E no corpo de `applySessionEnded`, após `markSessionEnded(...)` / `updateSessionStatus(...)`:

```ts
  const cli = sessionCli(session_id);
  const titleKey =
    status === "detached"
      ? "inbox.sessionDetached"
      : status === "failed"
        ? "inbox.sessionFailed"
        : "inbox.sessionCompleted";
  pushEvent({
    id: `session:${session_id}`,
    type: "session",
    titleKey,
    titleParams: { cli },
    ...(duration_secs != null
      ? { bodyKey: "inbox.sessionDuration", bodyParams: { duration: `${duration_secs}s` } }
      : {}),
    targetTab: "history",
  });
```

> O `pushEvent` precisa rodar para os DOIS ramos (detached e completed/failed) — colocar após o `if (status === "detached") {...return}` exigiria duplicar; refatorar para calcular o push antes do return do ramo detached, ou trocar o early-return por if/else.

Estender `useSessionEvents.test.ts` com um caso:

```ts
import { getInboxSnapshot, __resetForTests } from "../inbox/inboxStore";

// no beforeEach do describe existente (ou novo describe):
//   localStorage.clear(); __resetForTests();

it("pushes an inbox event when a session ends", () => {
  applySessionEnded({ session_id: "s1", status: "completed", exit_code: 0, duration_secs: 12 });
  const events = getInboxSnapshot().events;
  expect(events.find((e) => e.id === "session:s1")).toBeDefined();
  expect(events.find((e) => e.id === "session:s1")?.titleKey).toBe("inbox.sessionCompleted");
});
```

- [ ] **Step 2: Budget — no efeito de boot existente do `App.tsx`**

No `useEffect` que já chama `getBudgetAlerts` (linha ~101), após o `showToast(...)`, adicionar:

```ts
import { pushEvent } from "../features/inbox/inboxStore";
// ...
        const month = new Date().toISOString().slice(0, 7);
        for (const a of alerts) {
          pushEvent({
            id: `budget:${a.providerKey}:${month}`,
            type: "budget",
            titleKey: "inbox.budgetTitle",
            titleParams: { provider: a.providerKey },
            bodyKey: a.status === "exceeded" ? "inbox.budgetExceeded" : "inbox.budgetWarning",
            bodyParams: { percent: Math.round(a.percentUsed) },
            targetTab: "costs",
          });
        }
```

- [ ] **Step 3: Updates — no ChromeConnector (`App.tsx`)**

O ChromeConnector já tem `const { summary: updates } = useUpdates()`. Adicionar efeito:

```ts
  useEffect(() => {
    if (!updates) return;
    const all = [
      ...(updates.cli_updates ?? []),
      ...(updates.env_updates ?? []),
      ...(updates.tool_updates ?? []),
    ];
    for (const u of all) {
      if (!u.has_update || !u.latest) continue;
      pushEvent({
        id: `update:${u.cli}:${u.latest}`,
        type: "update",
        titleKey: "inbox.updateTitle",
        titleParams: { cli: u.cli, version: u.latest },
        targetTab: "updates",
      });
    }
  }, [updates]);
```

> Dedup garante: o mesmo `cli@version` re-push só re-marca como não lido — aceitável (1x por versão nova; o cache de updates é sessionStorage com TTL 1h, então não spamma a cada render).
> ATENÇÃO: re-push re-marca unread a cada boot do app enquanto o update não for instalado. Para evitar, mudar `pushEvent` para NÃO re-marcar unread quando `read===true` e o conteúdo é idêntico: comparar `titleParams` — se iguais, apenas atualizar `ts` mantendo `read`. Implementar essa regra no `pushEvent` (B1) já: `read: existing.read && sameContent ? existing.read : false`. Ajustar o teste B1 de dedup para cobrir: re-push com MESMO conteúdo mantém read; com conteúdo diferente re-marca unread. (O teste B1 do plano usa o mesmo evt — atualizar expectativa para `read: true` mantido — e adicionar caso com `titleParams` diferente esperando `read: false`.)

- [ ] **Step 4: Doctor — em `DoctorPage.tsx`**

No `runDiagnosis`, após `setItems(classified)`:

```ts
import { reportDoctorResults } from "../inbox/inboxStore";
// ...
      reportDoctorResults(
        results.map((r) => ({ key: r.key, name: r.name, installed: r.installed })),
      );
```

- [ ] **Step 5: Validar e commitar**

Run: `npx vitest run src/features/inbox src/features/history && npx tsc --noEmit && npm run build`
Expected: verde.

```bash
git add src/features/history/useSessionEvents.ts src/features/history/useSessionEvents.test.ts src/app/App.tsx src/features/workspace/DoctorPage.tsx src/features/inbox/
git commit -m "feat(inbox): wire session-ended, budget, updates and doctor sources with stable dedup keys"
```

---

## Frente C — Épico AA (INLINE — não delegar a workflow)

### Task C1: Contraste AA nos 7 temas + trava programática

**Files:**
- Create: `src/theme/contrast.ts`
- Modify: `src/theme/tokens.css`, `src/theme/theme-amber.css`, `theme-glacier.css`, `theme-light.css`, `theme-midnight.css`, `theme-phosphor.css`
- Modify: `src/theme/theme-contract.test.ts`

- [ ] **Step 1: Criar utilitário de contraste**

```ts
// src/theme/contrast.ts
// WCAG 2.x relative luminance + contrast ratio for #rrggbb values.
export function relativeLuminance(hex: string): number {
  const c = hex.replace("#", "");
  const [r, g, b] = [0, 2, 4]
    .map((i) => parseInt(c.slice(i, i + 2), 16) / 255)
    .map((v) => (v <= 0.03928 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4)));
  return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}

export function contrastRatio(fgHex: string, bgHex: string): number {
  const [l1, l2] = [relativeLuminance(fgHex), relativeLuminance(bgHex)].sort((a, b) => b - a);
  return (l1 + 0.05) / (l2 + 0.05);
}
```

- [ ] **Step 2: Estender `theme-contract.test.ts` (test-first — deve FALHAR antes do fix)**

O teste existente já tem um helper `loadCss()` que lê os arquivos de tema. Adicionar um describe novo que, para CADA tema de `THEME_IDS`:

1. Resolve o valor hex de cada token: valor no arquivo do tema, senão o default de `tokens.css` (regex `--<token>:\s*(#[0-9a-fA-F]{6})`).
2. Verifica os pares (texto vs superfície) com `contrastRatio`:

```ts
import { contrastRatio } from "./contrast";

const TEXT_TOKENS = ["--text", "--text-muted", "--text-dim", "--ok", "--warn", "--err"] as const;
const SURFACE_TOKENS = ["--bg", "--surface-1", "--surface-2"] as const;
const AA_MIN = 4.5;
const HC_TEXT_MIN = 7; // high-contrast theme aims AAA for plain text

describe("theme contract: WCAG AA contrast", () => {
  for (const theme of THEME_IDS) {
    it(`${theme}: text/semantic tokens reach >= 4.5:1 on every surface`, () => {
      const resolve = makeResolver(theme); // theme value ?? tokens.css default
      for (const textToken of TEXT_TOKENS) {
        const fg = resolve(textToken);
        for (const surfaceToken of SURFACE_TOKENS) {
          const bg = resolve(surfaceToken);
          const ratio = contrastRatio(fg, bg);
          const min = theme === "high-contrast" && textToken === "--text" ? HC_TEXT_MIN : AA_MIN;
          expect(
            ratio,
            `${theme}: ${textToken} (${fg}) vs ${surfaceToken} (${bg}) = ${ratio.toFixed(2)}`,
          ).toBeGreaterThanOrEqual(min);
        }
      }
    });
  }
});
```

`makeResolver(theme)`: função local no teste que parseia o CSS do tema e o `tokens.css` com a regex acima e retorna `(token) => hex`. Se um token não for hex puro (ex.: `rgba(...)`), lançar erro claro pedindo valor hex para os tokens do contrato.

Run: `npx vitest run src/theme/theme-contract.test.ts`
Expected: **FAIL** nos temas dark/amber/glacier/light/midnight/phosphor (valores atuais reprovam).

- [ ] **Step 3: Corrigir os tokens (valores pré-calculados, ratio pior-caso vs --surface-2)**

| Arquivo | Token | De | Para | Ratio novo |
|---|---|---|---|---|
| `tokens.css` (dark) | `--text-dim` | `#6b6b74` | `#7d7d87` | 4.63 |
| `theme-amber.css` | `--text-dim` | `#8a7a5c` | `#94835f` | 4.61 |
| `theme-glacier.css` | `--text-dim` | `#6e7e94` | `#5d6d85` | 4.89 |
| `theme-glacier.css` | `--ok` | `#1a7f37` | `#187534` | 4.91 |
| `theme-light.css` | `--text-dim` | `#7a7a7a` | `#6f6f6f` | 4.81 |
| `theme-light.css` | `--ok` | `#1a7f37` | `#187534` | 4.91 |
| `theme-light.css` | `--warn` | `#9a6700` | `#946300` | 4.72 |
| `theme-midnight.css` | `--text-dim` | `#756ea3` | `#847daf` | 4.63 |
| `theme-phosphor.css` | `--text-dim` | `#5e8a67` | `#639069` | 4.86 |

> Se o teste do Step 2 apontar OUTROS pares reprovando (ex.: `--err` em algum tema), corrigir com o mesmo método: ajustar lightness minimamente, manter o hue. O teste é a fonte da verdade, não esta tabela.

- [ ] **Step 4: Run tests until green**

Run: `npx vitest run src/theme/theme-contract.test.ts && npm test`
Expected: PASS (todos os temas)

- [ ] **Step 5: Commit**

```bash
git add src/theme/
git commit -m "fix(a11y): AA contrast for dim/semantic tokens across all 7 themes + programmatic contract guard"
```

### Task C2: Reativar `color-contrast` no axe e2e

**Files:**
- Modify: `e2e/launcher.spec.ts:70-77`

- [ ] **Step 1: Remover a exclusão**

Trocar o builder no teste de a11y por:

```ts
    const results = await new AxeBuilder({ page })
      .withTags(["wcag2a", "wcag2aa"])
      .analyze();
```

(remover `.disableRules(["color-contrast"])` e o comentário TODO).

- [ ] **Step 2: Rodar o e2e localmente**

```bash
npm run dev   # em background, esperar HTTP 200 em http://localhost:5173
npx playwright test
```

Expected: 3 passed. Se `color-contrast` reprovar em elemento específico (ex.: placeholder de input, texto sobre accent), corrigir o token/CSS do elemento e re-rodar — NÃO re-desabilitar a regra.

- [ ] **Step 3: Commit**

```bash
git add e2e/launcher.spec.ts
git commit -m "test(a11y): re-enable axe color-contrast rule — AA debt paid"
```

> CI: o job `e2e` do `quality.yml` já roda este spec; nenhuma mudança de workflow é necessária.

### Task C3: ARIA combobox no Command Palette

**Files:**
- Modify: `src/features/command-palette/CommandPalette.tsx`

- [ ] **Step 1: Padrão combobox no input e listbox**

1. No `<input>` (linha ~471), adicionar:

```tsx
            role="combobox"
            aria-expanded="true"
            aria-controls="cd-cmd-listbox"
            aria-autocomplete="list"
            aria-activedescendant={
              flatRows[selectedIndex] ? `cd-cmd-opt-${flatRows[selectedIndex].id}` : undefined
            }
```

2. No container da lista (`<div className="cd-cmd__list" ... role="listbox">`), adicionar `id="cd-cmd-listbox"`.
3. Nos grupos (`<div key={section.key} className="cd-cmd__group">`), adicionar `role="group"` e `aria-labelledby={`cd-cmd-group-${section.key}`}`; no heading do grupo (`cd-cmd__group-heading`) adicionar `id={`cd-cmd-group-${section.key}`}` e `role="presentation"`.
4. Em cada `<button ... role="option">` adicionar `id={`cd-cmd-opt-${row.id}`}`.

- [ ] **Step 2: Retorno de foco ao fechar**

No componente, capturar o elemento focado na abertura e restaurar no fechamento:

```tsx
  const previousFocusRef = useRef<HTMLElement | null>(null);

  useEffect(() => {
    if (open) {
      previousFocusRef.current = document.activeElement as HTMLElement | null;
      setQuery("");
      setSelectedIndex(0);
      requestAnimationFrame(() => inputRef.current?.focus());
    } else if (previousFocusRef.current) {
      previousFocusRef.current.focus?.();
      previousFocusRef.current = null;
    }
  }, [open]);
```

(substitui o `useEffect` existente de open — manter o reset de query/seleção.)

- [ ] **Step 3: Validar com axe**

```bash
npx tsc --noEmit && npx playwright test
```

Expected: verde (o spec a11y pega `aria-*` inválido/role misuse).

- [ ] **Step 4: Commit**

```bash
git add src/features/command-palette/CommandPalette.tsx
git commit -m "feat(a11y): full ARIA combobox pattern + focus restore in command palette"
```

### Task C4: h1 por página + hierarquia de headings

**Files (11 páginas):** `src/features/admin/AdminPage.tsx:34`, `src/features/costs/CostsPage.tsx:84`, `src/features/help/HelpPage.tsx:71`, `src/features/history/HistoryPage.tsx:348`, `src/features/launcher/LauncherPage.tsx:153`, `src/features/mcp/McpPage.tsx:155`, `src/features/prereqs/PrereqsPage.tsx:18`, `src/features/tools/ToolsPage.tsx:70`, `src/features/updates/UpdatesPage.tsx:83`, `src/features/workspace/DoctorPage.tsx:117`, `src/features/workspace/WorkspacePage.tsx:191`
**Também:** `src/features/workspace/WorkspacePage.tsx:736` (h1 duplicado), promoção de h3→h2 nas seções de página.

- [ ] **Step 1: Promover os títulos de página**

Nos 11 arquivos, trocar `<h2 className="cd-page__title">` por `<h1 className="cd-page__title">` (e o `</h2>` correspondente por `</h1>`). A classe carrega o estilo — visual inalterado.

- [ ] **Step 2: Corrigir o h1 duplicado do WorkspacePage**

Em `src/features/workspace/WorkspacePage.tsx:736`, trocar `<h1 className="cd-ws-form__title">` por `<h2 className="cd-ws-form__title">`.

- [ ] **Step 3: Promover seções h3→h2 nas páginas (hierarquia h1 → h2)**

Trocar `<h3 ...>` por `<h2 ...>` (mantendo as classes) nestes pontos:

- `src/features/admin/sections/CliOverridesSection.tsx:110`, `CustomIdesSection.tsx:62`, `PresetsSection.tsx:72`, `ProvidersSection.tsx:127` (`cd-admin-section__title`)
- `src/features/costs/BudgetDashboard.tsx:196` e `src/features/costs/CostsPage.tsx` (`cd-costs__section` — incluindo os criados na Task A3)
- `src/features/help/HelpPage.tsx:80,85,101,113,123,139` (`cd-help__heading`)
- `src/features/launcher/LauncherPage.tsx:178` (`cd-launcher__section-title`)
- `src/features/mcp/McpPage.tsx:195,221` (`cd-mcp__group-title`)
- `src/features/updates/AppUpdater.tsx:19` e `UpdatesPage.tsx:219` (`cd-updates__section-title`)
- `src/features/workspace/RunbookRunner.tsx:237` (`cd-costs__section`)

NÃO mexer em: `ui/EmptyState.tsx`, `ui/ConfirmDialog.tsx`, `ui/Dialog.tsx`, `ui/ErrorBoundary.tsx`, `OnboardingPage.tsx` (página própria, já tem h1), `RunbooksPanel.tsx` (já h2), `DoctorPage.tsx:235` (já h2).

- [ ] **Step 4: Verificar que nenhum CSS seleciona por elemento**

```bash
grep -rn "h2\.\|h3\.\| h2 \| h3 " src/features/*.css src/features/**/*.css src/ui/*.css | grep -v "__"
```

Expected: nenhum seletor de elemento para esses headings (estilos são por classe). Se houver, atualizar o seletor.

- [ ] **Step 5: Validar e commitar**

```bash
npx tsc --noEmit && npm test && npx playwright test
git add src/features/
git commit -m "feat(a11y): one h1 per page + coherent heading hierarchy (h1>h2)"
```

---

## Encerramento D (INLINE)

### Task D1: Bump 16.0.0

**Files:**
- Modify: `package.json` (`"version": "15.2.6"` → `"16.0.0"`)
- Modify: `src-tauri/tauri.conf.json` (campo `version`)
- Modify: `src-tauri/Cargo.toml` (`version = "15.2.6"` → `"16.0.0"`)

- [ ] **Step 1: Editar os 3 arquivos** (procurar `15.2.6` em cada um; em `tauri.conf.json` o campo fica no topo do JSON).

- [ ] **Step 2: Regenerar locks**

```bash
npm install --legacy-peer-deps          # atualiza package-lock.json
cd src-tauri && cargo check && cd ..    # atualiza Cargo.lock
```

- [ ] **Step 3: Verificar e commitar**

```bash
grep -rn "15\.2\.6" package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml
# Expected: nenhum resultado
git add package.json package-lock.json src-tauri/tauri.conf.json src-tauri/Cargo.toml src-tauri/Cargo.lock
git commit -m "chore(release): bump version to 16.0.0"
```

### Task D2: CHANGELOG + validação final completa

**Files:**
- Modify: `CHANGELOG.md` (nova seção 16.0.0 no topo — escrever DO ZERO com base em `git log main..HEAD --oneline`, não confiar em esboço pré-existente)

- [ ] **Step 1: Gerar a lista real de mudanças**

```bash
git log main..HEAD --oneline
```

Escrever a seção `## [16.0.0] - 2026-06-11` agrupando por: Security (alpha), Analytics & Budget (beta1), MCP Manager / Runbooks / Profiles / Themes (beta2), Agent Analytics / Agent Inbox / Accessibility AA (v16.0). Citar somente o que está nos commits.

- [ ] **Step 2: Validação final completa**

```bash
npm run build && npx tsc --noEmit && npm test
cd src-tauri && cargo test && cargo clippy --no-deps -- -D warnings && cd ..
npx playwright test
npm run tauri build
```

Expected: tudo verde; bundles `AI Launcher_16.0.0_x64_pt-BR.msi` e `AI Launcher_16.0.0_x64-setup.exe` gerados.

- [ ] **Step 3: Smoke test do exe** (fechar instância antiga primeiro — single-instance!)

```powershell
Get-Process ai-launcher -ErrorAction SilentlyContinue | ForEach-Object { $_.CloseMainWindow() }
Start-Sleep 3
Start-Process "src-tauri\target\release\ai-launcher.exe"
# verificar: janela abre, título correto, sino do Inbox visível na TopBar
```

- [ ] **Step 4: Commit final**

```bash
git add CHANGELOG.md
git commit -m "docs(changelog): 16.0.0 — analytics, inbox, accessibility AA"
```

---

## Self-review do plano (executado na escrita)

- **Cobertura da spec:** Analytics (A1-A3 ✓), Inbox 4 fontes+dedup+cap+UI (B1-B3 ✓), AA contraste+trava+axe+combobox+h1+foco (C1-C4 ✓; "axe no CI" já existia — verificado), bump+changelog+validação (D1-D2 ✓). Distribuição: cortada por decisão do usuário ✓.
- **Consistência de tipos:** `RankRow.label: string | null` ↔ `BarListItem.label: string | null` + `fallbackLabel` ✓; `PushInput`/`InboxEvent` ↔ usos em B2/B3 ✓; `DoctorResult` ↔ `PrereqCheck` mapeado ✓.
- **Riscos sinalizados no próprio plano:** dedup re-unread (B3 Step 3 ajusta regra do B1), early-return do detached (B3 Step 1), h3→h2 da A3 se C4 rodar antes (nota na A3), tabela de contraste é ponto de partida — o teste é a fonte da verdade (C1 Step 3).
