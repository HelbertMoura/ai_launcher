# AI Launcher Pro v15.2 — Antigravity CLI + IDE Version + DnD Fix — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir a detecção do Antigravity CLI (binário real é `agy`, não `antigravity`), remover o Gemini descontinuado, ler a versão do Antigravity IDE Electron via `ProductVersion` do `.exe`, e consertar o drag-and-drop de CLIs na Dashboard com um handle visual dedicado.

**Architecture:** O backend Rust ganha dois campos opcionais no struct `CliInfo` (`extra_paths`, `update_manifest_url`) para suportar CLIs instalados por script (paths arbitrários + manifesto JSON de update) sem hardcode. Uma nova função `read_exe_product_version` lê metadados PE via PowerShell, generalizando para todos os IDEs Electron (Cursor, Windsurf, Antigravity). No frontend, a lógica de DnD é extraída para `useDraggable.ts` e o gesto de arrastar é movido para um handle dedicado `⋮⋮` no cabeçalho do card, eliminando o conflito de `mousedown` com botões filhos.

**Tech Stack:** Rust + Tauri v2 backend; React 18 + TypeScript frontend; PowerShell para leitura de PE metadata; HTTP via `ureq` para manifesto JSON.

**Spec:** [`docs/superpowers/specs/2026-05-19-v15.2-antigravity-cli-design.md`](../specs/2026-05-19-v15.2-antigravity-cli-design.md) — commit `0f2850a`

---

## File Structure

### Created
- `src/hooks/useDraggable.ts` — hook React reutilizável (state + handlers de drag/drop)

### Modified — Backend (Rust)
- `src-tauri/src/util.rs` — struct `CliInfo`, `get_cli_definitions()`, `resolve_cli_path_win()`, `resolve_windows_cmd()`, nova função `read_exe_product_version()`, novos testes
- `src-tauri/src/commands/tools.rs` — fallback de versão via `read_exe_product_version` em `check_tools()`
- `src-tauri/src/commands/cli.rs` — usar `update_manifest_url` em `check_cli_updates()`
- `src-tauri/src/commands/updates.rs` — fallback `read_exe_product_version` em `check_tool_updates()`

### Modified — Frontend
- `src/features/launcher/useClis.ts` — adicionar campos `extra_paths`, `update_manifest_url` à interface TS
- `src/features/launcher/LauncherPage.tsx` — substituir lógica inline de DnD por `useDraggable`
- `src/features/launcher/CliCard.tsx` — adicionar drag handle, mover `draggable` para o handle
- `src/features/launcher/CustomCliCard.tsx` — idem
- `src/features/launcher/LauncherPage.css` — estilos do `.cd-drag-handle` e `.cd-draggable-item--drop-target`
- `src/i18n/locales/en.ts` — remover refs a Gemini, adicionar `launcher.dragToReorder`
- `src/i18n/locales/pt-BR.ts` — idem
- `src/lib/iconRegistry.ts` — remover `"gemini"` de `CLI_KEYS`, adicionar `"antigravity"` (já existe em `hasCliIcon`)
- `src/icons/registry.ts` — remover `"gemini"` de `CLI_PNG` e `CLI_KEYS`
- `src/features/launcher/LaunchDialog.tsx:29` — remover `"gemini"` de `CLIS_WITH_PROMPT_FLAG`
- `src/features/help/AnimatedTerminal.tsx` — remover linhas de Gemini do terminal animado

### Modified — Docs & Memory
- `CHANGELOG.md` — substituir entrada esboço v15.2.0 pela final
- `.wolf/cerebrum.md` — Decision Log + Key Learnings
- `.wolf/buglog.json` — bug-001 (definição CLI Antigravity incorreta)
- `.wolf/anatomy.md` — atualizar via append (novo `useDraggable.ts`)

### Removed
- `public/icons/cli/gemini.svg` — ícone descontinuado
- `public/icons/cli/gemini.png` — idem

---

## Task 1: Backend — Adicionar campos `extra_paths` e `update_manifest_url` ao struct `CliInfo`

**Files:**
- Modify: `src-tauri/src/util.rs:16-28` (struct `CliInfo`)
- Modify: `src-tauri/src/util.rs:82-193` (todas as entradas em `get_cli_definitions`)

- [ ] **Step 1: Escrever o teste falhando que valida campos novos**

Adicionar no fim de `src-tauri/src/util.rs` dentro do bloco `#[cfg(test)] mod tests`:

```rust
#[test]
fn cli_info_has_extra_paths_and_manifest_url() {
    let cli = CliInfo {
        key: "x".into(),
        name: "X".into(),
        command: "x".into(),
        flag: None,
        install_cmd: "".into(),
        version_cmd: "".into(),
        npm_pkg: None,
        pip_pkg: None,
        install_method: "script".into(),
        install_url: None,
        extra_paths: vec!["%LOCALAPPDATA%\\x\\x.exe".into()],
        update_manifest_url: Some("https://example.com/m.json".into()),
    };
    assert_eq!(cli.extra_paths.len(), 1);
    assert!(cli.update_manifest_url.is_some());
}
```

- [ ] **Step 2: Rodar o teste e ver que falha**

Run: `cargo test --manifest-path src-tauri/Cargo.toml cli_info_has_extra_paths_and_manifest_url`
Expected: FAIL com `error[E0560]: struct \`CliInfo\` has no field named \`extra_paths\`` (e idem `update_manifest_url`).

- [ ] **Step 3: Estender o struct `CliInfo`**

Em `src-tauri/src/util.rs:16-28`, substituir o bloco struct atual por:

```rust
#[derive(Debug, Serialize, Deserialize, Clone)]
pub struct CliInfo {
    pub key: String,
    pub name: String,
    pub command: String,
    pub flag: Option<String>,
    pub install_cmd: String,
    pub version_cmd: String,
    pub npm_pkg: Option<String>,
    pub pip_pkg: Option<String>,
    pub install_method: String,
    pub install_url: Option<String>,
    #[serde(default)]
    pub extra_paths: Vec<String>,
    #[serde(default)]
    pub update_manifest_url: Option<String>,
}
```

- [ ] **Step 4: Inicializar os novos campos em TODAS as entradas existentes de `get_cli_definitions`**

Para cada uma das entradas em `src-tauri/src/util.rs:82-193` (claude, codex, gemini, qwen, kilocode, opencode, crush, droid, antigravity), adicionar antes do `}` de fechamento da struct:

```rust
            extra_paths: vec![],
            update_manifest_url: None,
```

Exemplo aplicado ao claude:

```rust
CliInfo {
    key: "claude".into(),
    name: "Claude".into(),
    command: "claude".into(),
    flag: Some("--dangerously-skip-permissions".into()),
    install_cmd: "npm install -g @anthropic-ai/claude-code".into(),
    version_cmd: "claude --version".into(),
    npm_pkg: Some("@anthropic-ai/claude-code".into()),
    pip_pkg: None,
    install_method: "npm".into(),
    install_url: None,
    extra_paths: vec![],
    update_manifest_url: None,
},
```

Aplicar análogo nas 8 outras entradas. Não tocar nos valores existentes — só acrescentar os 2 campos.

- [ ] **Step 5: Rodar todos os testes do crate Rust**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS — todos os testes existentes + o novo.

- [ ] **Step 6: Rodar clippy estrito**

Run: `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
Expected: sem warnings.

- [ ] **Step 7: Commit**

```bash
git add src-tauri/src/util.rs
git commit -m "feat(util): add extra_paths and update_manifest_url to CliInfo

Generaliza CliInfo para suportar CLIs instalados por script com paths
absolutos e manifesto JSON de update (preparação para agy/Antigravity).
Campos opcionais com #[serde(default)] mantêm compat."
```

---

## Task 2: Backend — Substituir entrada `antigravity` e remover entrada `gemini`

**Files:**
- Modify: `src-tauri/src/util.rs:108-119` (remover gemini)
- Modify: `src-tauri/src/util.rs:180-191` (substituir antigravity — linha pode mudar após Task 1)

- [ ] **Step 1: Escrever testes falhando**

Adicionar em `#[cfg(test)] mod tests`:

```rust
#[test]
fn cli_definitions_have_no_gemini() {
    let defs = get_cli_definitions();
    assert!(
        !defs.iter().any(|c| c.key == "gemini"),
        "Gemini CLI foi descontinuado e não deve estar em get_cli_definitions"
    );
}

#[test]
fn antigravity_cli_uses_agy_command() {
    let defs = get_cli_definitions();
    let ag = defs
        .iter()
        .find(|c| c.key == "antigravity")
        .expect("antigravity deve estar em get_cli_definitions");
    assert_eq!(ag.command, "agy", "binário real do Antigravity CLI é 'agy'");
    assert_eq!(ag.install_method, "script");
    assert!(
        ag.extra_paths
            .iter()
            .any(|p| p.contains(r"\agy\bin\agy.exe")),
        "extra_paths deve incluir %LOCALAPPDATA%\\agy\\bin\\agy.exe"
    );
    assert!(
        ag.update_manifest_url
            .as_deref()
            .map(|u| u.contains("manifests/windows_amd64.json"))
            .unwrap_or(false),
        "update_manifest_url deve apontar para o manifesto Google"
    );
}
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cargo test --manifest-path src-tauri/Cargo.toml cli_definitions_have_no_gemini antigravity_cli_uses_agy_command`
Expected: FAIL — gemini ainda existe; antigravity command ainda é "antigravity" e extra_paths está vazio.

- [ ] **Step 3: Remover a entrada gemini**

Em `src-tauri/src/util.rs`, localizar o bloco que começa com `key: "gemini".into(),` e remover do `CliInfo {` ao `},` inclusive. São 12 linhas. Salvar.

- [ ] **Step 4: Substituir a entrada antigravity**

Localizar o bloco que começa com `key: "antigravity".into(),` e substituir o bloco completo por:

```rust
CliInfo {
    key: "antigravity".into(),
    name: "Antigravity".into(),
    command: "agy".into(),
    flag: None, // confirmar com `agy help`; se houver flag de skip-permissions, atualizar
    install_cmd: "iwr https://antigravity.google/cli/install.ps1 -useb | iex".into(),
    version_cmd: "agy --version".into(),
    npm_pkg: None,
    pip_pkg: None,
    install_method: "script".into(),
    install_url: Some("https://antigravity.google/download".into()),
    extra_paths: vec!["%LOCALAPPDATA%\\agy\\bin\\agy.exe".into()],
    update_manifest_url: Some(
        "https://antigravity-cli-auto-updater-974169037036.us-central1.run.app/manifests/windows_amd64.json".into(),
    ),
},
```

- [ ] **Step 5: Rodar testes**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS.

- [ ] **Step 6: Validar a flag do agy manualmente**

Run (PowerShell, fora do `cargo`):

```powershell
& "$env:LOCALAPPDATA\agy\bin\agy.exe" help
```

Expected: stdout com lista de comandos/flags. Se aparecer flag de skip-permissions (ex: `--yolo`, `--yes`, `--auto`), editar `src-tauri/src/util.rs` substituindo `flag: None` por `flag: Some("--<flag>".into())` e rodar `cargo test` novamente. Caso contrário, manter `None`.

- [ ] **Step 7: Commit**

```bash
git add src-tauri/src/util.rs
git commit -m "feat(util): drop Gemini, fix Antigravity CLI to use 'agy' binary

- Remove Gemini CLI (descontinuado)
- Antigravity CLI agora usa o binário real 'agy' instalado em
  %LOCALAPPDATA%\\agy\\bin\\agy.exe via script PowerShell oficial
- install_method=script; update_manifest_url para JSON oficial Google"
```

---

## Task 3: Backend — Resolver `agy.exe` via `extra_paths` em `resolve_cli_path_win`

**Files:**
- Modify: `src-tauri/src/util.rs:236-243` (`resolve_windows_cmd`)
- Modify: `src-tauri/src/util.rs:676-712` (`resolve_cli_path_win`)
- Modify: `src-tauri/src/util.rs:384-416` (`get_installed_version`)

- [ ] **Step 1: Limpar `resolve_windows_cmd` removendo `antigravity`**

Em `src-tauri/src/util.rs:236-243`, substituir:

```rust
pub fn resolve_windows_cmd(cmd: &str) -> String {
    match cmd {
        "npm" | "pnpm" | "yarn" | "pip" | "tauri" | "bun" | "code" | "cursor" | "windsurf" | "antigravity" => {
            format!("{}.cmd", cmd)
        }
        _ => cmd.to_string(),
    }
}
```

Por:

```rust
pub fn resolve_windows_cmd(cmd: &str) -> String {
    match cmd {
        "npm" | "pnpm" | "yarn" | "pip" | "tauri" | "bun" | "code" | "cursor" | "windsurf" => {
            format!("{}.cmd", cmd)
        }
        _ => cmd.to_string(),
    }
}
```

(Remove `"antigravity"` — `agy` é `.exe`, não `.cmd`.)

- [ ] **Step 2: Estender `resolve_cli_path_win` para aceitar `extra_paths`**

Substituir a assinatura e o corpo de `resolve_cli_path_win` em `src-tauri/src/util.rs:676-712`. Substituir por:

```rust
pub fn resolve_cli_path_win(cmd: &str, extra_paths: &[String]) -> Option<String> {
    // 1) Caminhos absolutos extras (com expand_env) — prioridade máxima
    for raw in extra_paths {
        let expanded = expand_env(raw);
        if std::path::Path::new(&expanded).exists() {
            return Some(expanded);
        }
    }
    // 2) Caminhos npm conhecidos
    for prefix in [r"%APPDATA%\npm\", r"%LOCALAPPDATA%\npm\"] {
        let base = expand_env(prefix);
        for ext in &["cmd", "exe", "ps1", "bat"] {
            let full = format!("{}{}.{}", base, cmd, ext);
            if std::path::Path::new(&full).exists() {
                return Some(full);
            }
        }
    }
    // 3) Fallback: `where`
    let where_results: Vec<String> = {
        let mut c = Command::new("where");
        c.arg(cmd);
        c.creation_flags(CREATE_NO_WINDOW);
        c.output()
            .ok()
            .filter(|o| o.status.success())
            .map(|o| String::from_utf8_lossy(&o.stdout).to_string())
            .map(|s| {
                s.lines()
                    .map(|l| l.trim().to_string())
                    .filter(|l| !l.is_empty() && std::path::Path::new(l).exists())
                    .collect()
            })
            .unwrap_or_default()
    };

    for path in &where_results {
        if !path.to_lowercase().contains(r"\.local\bin\") {
            return Some(path.clone());
        }
    }
    if let Some(first) = where_results.first() {
        return Some(first.clone());
    }
    None
}
```

- [ ] **Step 3: Atualizar os call sites de `resolve_cli_path_win`**

Rodar para localizar todos os usos:

Run: `grep -rn "resolve_cli_path_win(" src-tauri/src/`
Expected: 3 ocorrências — `util.rs:406` (em `get_installed_version`), `cli.rs:268` (em `launch_cli`), `cli.rs:387` (em `launch_custom_cli`).

Editar `src-tauri/src/util.rs:406` (linha pode variar): mudar

```rust
    if let Some(resolved) = resolve_cli_path_win(&cli.command) {
```

para

```rust
    if let Some(resolved) = resolve_cli_path_win(&cli.command, &cli.extra_paths) {
```

Editar `src-tauri/src/commands/cli.rs:268`: mudar

```rust
    let resolved_cmd = resolve_cli_path_win(&cli.command).unwrap_or_else(|| cli.command.clone());
```

para

```rust
    let resolved_cmd = resolve_cli_path_win(&cli.command, &cli.extra_paths).unwrap_or_else(|| cli.command.clone());
```

Editar `src-tauri/src/commands/cli.rs:387` (dentro de `launch_custom_cli`, sem CliInfo): mudar

```rust
    let resolved_cmd = resolve_cli_path_win(&command).unwrap_or_else(|| command.clone());
```

para

```rust
    let resolved_cmd = resolve_cli_path_win(&command, &[]).unwrap_or_else(|| command.clone());
```

- [ ] **Step 4: Adicionar teste para o caminho `extra_paths`**

Em `#[cfg(test)] mod tests` de `src-tauri/src/util.rs`:

```rust
#[test]
fn resolve_cli_path_uses_extra_paths_when_file_exists() {
    use std::io::Write;
    let tmp = std::env::temp_dir().join("ai-launcher-test-agy");
    let _ = std::fs::create_dir_all(&tmp);
    let exe = tmp.join("fake-agy.exe");
    let mut f = std::fs::File::create(&exe).expect("criar fake exe");
    let _ = f.write_all(b"fake");
    let extras = vec![exe.to_string_lossy().to_string()];
    let resolved = resolve_cli_path_win("nonexistent-cmd-xyz", &extras);
    assert_eq!(resolved.as_deref(), Some(exe.to_string_lossy().as_ref()));
    let _ = std::fs::remove_file(&exe);
}
```

- [ ] **Step 5: Rodar testes**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS.

- [ ] **Step 6: Rodar clippy**

Run: `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
Expected: sem warnings.

- [ ] **Step 7: Commit**

```bash
git add src-tauri/src/util.rs src-tauri/src/commands/cli.rs
git commit -m "feat(util): resolve_cli_path_win consulta extra_paths primeiro

extra_paths tem prioridade sobre %APPDATA%\\npm e PATH para CLIs
script-installed como agy. Atualiza call sites e remove 'antigravity'
de resolve_windows_cmd (não é .cmd)."
```

---

## Task 4: Backend — `read_exe_product_version` para IDEs Electron

**Files:**
- Modify: `src-tauri/src/util.rs` (nova função, antes da seção `// ============ HELPERS — STRINGS / VERSIONS ============`)

- [ ] **Step 1: Escrever o teste falhando**

Em `#[cfg(test)] mod tests`:

```rust
#[test]
fn read_exe_product_version_returns_none_for_missing_file() {
    let p = std::path::Path::new(r"C:\__nonexistent__\fake.exe");
    assert_eq!(read_exe_product_version(p), None);
}
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cargo test --manifest-path src-tauri/Cargo.toml read_exe_product_version_returns_none_for_missing_file`
Expected: FAIL com `cannot find function \`read_exe_product_version\``.

- [ ] **Step 3: Implementar a função**

Adicionar em `src-tauri/src/util.rs`, logo após a função `extract_version` (linhas ~367-382):

```rust
/// Lê a `ProductVersion` dos metadados de um executável PE (Windows).
/// Usa PowerShell para evitar deps nativas. Retorna `None` se o arquivo não existir
/// ou não tiver metadados de versão.
pub fn read_exe_product_version(path: &std::path::Path) -> Option<String> {
    if !path.exists() {
        return None;
    }
    let escaped = path.to_string_lossy().replace('\'', "''");
    let ps = format!(
        "(Get-Item -LiteralPath '{}').VersionInfo.ProductVersion",
        escaped
    );
    let (ok, out) = run_silent("powershell", &["-NoProfile", "-Command", &ps]);
    if !ok {
        return None;
    }
    out.as_deref().and_then(extract_version)
}
```

- [ ] **Step 4: Rodar teste**

Run: `cargo test --manifest-path src-tauri/Cargo.toml read_exe_product_version_returns_none_for_missing_file`
Expected: PASS.

- [ ] **Step 5: Smoke test manual com o Antigravity.exe real**

Run (PowerShell):

```powershell
(Get-Item "$env:LOCALAPPDATA\Programs\Antigravity\Antigravity.exe").VersionInfo.ProductVersion
```

Expected: imprime `2.0.0` (ou similar). Confirma que a chamada subjacente que `read_exe_product_version` faz produz o resultado esperado.

- [ ] **Step 6: Rodar suite completa**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS em tudo.

- [ ] **Step 7: Commit**

```bash
git add src-tauri/src/util.rs
git commit -m "feat(util): add read_exe_product_version for Electron IDE versioning

Electron apps (Antigravity, Cursor, Windsurf) não respondem a --version.
A versão real fica em ProductVersion do PE, lida via PowerShell."
```

---

## Task 5: Backend — Fallback de versão em `check_tools`

**Files:**
- Modify: `src-tauri/src/commands/tools.rs:15-56` (`check_tools`)
- Modify: `src-tauri/src/commands/tools.rs:3-7` (import de `read_exe_product_version`)

- [ ] **Step 1: Atualizar import**

Em `src-tauri/src/commands/tools.rs:3-7`, substituir:

```rust
use crate::util::{
    command_exists, encode_powershell_command, extract_version, find_tool_path,
    find_windows_terminal, get_tool_definitions, resolve_windows_cmd, run_silent,
    user_home_dir_string, validate_directory, CheckResult, ToolInfo, CREATE_NO_WINDOW,
};
```

Por:

```rust
use crate::util::{
    command_exists, encode_powershell_command, extract_version, find_tool_path,
    find_windows_terminal, get_tool_definitions, read_exe_product_version,
    resolve_windows_cmd, run_silent, user_home_dir_string, validate_directory,
    CheckResult, ToolInfo, CREATE_NO_WINDOW,
};
```

- [ ] **Step 2: Atualizar `check_tools` com fallback**

Em `src-tauri/src/commands/tools.rs:15-56`, substituir o corpo inteiro da função `check_tools` por:

```rust
#[tauri::command]
pub fn check_tools() -> Vec<CheckResult> {
    get_tool_definitions()
        .iter()
        .map(|tool| {
            let cmd_in_path = command_exists(&tool.command);
            let path = find_tool_path(&tool.key);
            let has_binary = path.is_some();
            let installed = cmd_in_path || has_binary;

            // Estratégia: tenta --version (via cmd ou via binário), depois fallback
            // para ProductVersion do PE (Electron apps não respondem a --version).
            let version = if cmd_in_path {
                let parts: Vec<&str> = tool.version_cmd.split_whitespace().collect();
                let (_, raw) = if parts.len() > 1 {
                    run_silent(&tool.command, &parts[1..])
                } else {
                    run_silent(&tool.command, &["--version"])
                };
                let from_cmd = raw.as_deref().and_then(extract_version);
                from_cmd.or_else(|| {
                    path.as_deref()
                        .and_then(read_exe_product_version)
                })
            } else if let Some(ref p) = path {
                let p_str = p.to_string_lossy().to_string();
                let parts: Vec<&str> = tool.version_cmd.split_whitespace().collect();
                let (_, raw) = if parts.len() > 1 {
                    run_silent(&p_str, &parts[1..])
                } else {
                    run_silent(&p_str, &["--version"])
                };
                let from_cmd = raw.as_deref().and_then(extract_version);
                from_cmd.or_else(|| read_exe_product_version(p))
            } else {
                None
            };

            // Garante chip "detectado" quando instalado mas sem versão extraída.
            let version = if installed && version.is_none() {
                Some("detectado".into())
            } else {
                version
            };

            CheckResult {
                key: tool.key.clone(),
                name: tool.name.clone(),
                installed,
                version,
                install_command: Some(tool.install_hint.clone()),
            }
        })
        .collect()
}
```

- [ ] **Step 3: Compilar**

Run: `cargo build --manifest-path src-tauri/Cargo.toml`
Expected: sem erros.

- [ ] **Step 4: Rodar clippy**

Run: `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
Expected: sem warnings.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/commands/tools.rs
git commit -m "feat(tools): fallback para ProductVersion quando --version falha

IDEs Electron (Antigravity, Cursor, Windsurf) não respondem a
--version. O fallback lê os metadados PE para mostrar a versão real."
```

---

## Task 6: Backend — Usar `update_manifest_url` em `check_cli_updates`

**Files:**
- Modify: `src-tauri/src/commands/cli.rs:43-67` (`check_cli_updates`)
- Modify: `src-tauri/src/commands/cli.rs:5-10` (import de `http_agent`)
- Modify: `src-tauri/src/commands/updates.rs:54-93` (`check_tool_updates` — também ganha o fallback ProductVersion)

- [ ] **Step 1: Adicionar helper `fetch_manifest_version` em `util.rs`**

Adicionar em `src-tauri/src/util.rs`, logo após `fetch_github_latest` (~linha 674):

```rust
/// Faz GET em uma URL JSON e retorna o campo `.version` como SemVer.
/// Usado para CLIs script-installed que publicam manifesto público.
pub fn fetch_manifest_version(url: &str) -> Option<String> {
    let resp = http_agent().get(url).call().ok()?;
    let json: serde_json::Value = resp.into_json().ok()?;
    json["version"]
        .as_str()
        .map(|s| s.trim_start_matches('v').to_string())
}
```

- [ ] **Step 2: Atualizar import em `commands/cli.rs`**

Em `src-tauri/src/commands/cli.rs:5-10`, substituir:

```rust
use crate::util::{
    check_cli_installed, compare_versions, encode_powershell_command, find_windows_terminal,
    get_cli_definitions, get_installed_version, log_event, npm_latest, resolve_cli_path_win,
    sanitize_args, stream_install, validate_directory, CheckResult, CliInfo,
    DEFAULT_INSTALL_TIMEOUT_SEC,
};
```

Por:

```rust
use crate::util::{
    check_cli_installed, compare_versions, encode_powershell_command, fetch_manifest_version,
    find_windows_terminal, get_cli_definitions, get_installed_version, log_event, npm_latest,
    resolve_cli_path_win, sanitize_args, stream_install, validate_directory, CheckResult,
    CliInfo, DEFAULT_INSTALL_TIMEOUT_SEC,
};
```

- [ ] **Step 3: Substituir `check_cli_updates` para usar `update_manifest_url`**

Em `src-tauri/src/commands/cli.rs:43-67`, substituir o corpo da função inteira por:

```rust
#[tauri::command]
pub fn check_cli_updates() -> Vec<crate::util::UpdateInfo> {
    use crate::util::UpdateInfo;
    get_cli_definitions()
        .into_iter()
        .map(|cli| {
            let current = get_installed_version(&cli);
            let latest = if let Some(ref url) = cli.update_manifest_url {
                fetch_manifest_version(url)
            } else {
                cli.npm_pkg.as_ref().and_then(|p| npm_latest(p))
            };
            let has_update = match (&current, &latest) {
                (Some(c), Some(l)) => compare_versions(c, l),
                _ => false,
            };
            let no_api = latest.is_none();
            UpdateInfo {
                cli: cli.name,
                current,
                latest,
                has_update,
                method: cli.install_method.clone(),
                no_api,
                key: None,
            }
        })
        .collect()
}
```

- [ ] **Step 4: Aplicar fallback ProductVersion também em `check_tool_updates`**

Em `src-tauri/src/commands/updates.rs:4-9`, adicionar `read_exe_product_version` ao import:

```rust
use crate::util::{
    chrono_format_local_now, command_exists, compare_versions, detect_python, extract_version,
    fetch_github_latest, fetch_vscode_latest, find_tool_path, find_windows_terminal,
    get_tool_definitions, http_agent, npm_latest, read_exe_product_version, run_silent,
    stream_install, CheckResult, UpdateInfo, UpdatesSummary,
};
```

Em `src-tauri/src/commands/updates.rs:54-93`, substituir o corpo de `check_tool_updates`:

```rust
#[tauri::command]
pub fn check_tool_updates() -> Vec<UpdateInfo> {
    get_tool_definitions()
        .iter()
        .filter_map(|tool| {
            let cmd_in_path = command_exists(&tool.command);
            let path = find_tool_path(&tool.key);
            let has_binary = path.is_some();
            if !cmd_in_path && !has_binary {
                return None;
            }
            let current = if cmd_in_path {
                let (_, v) = run_silent(&tool.command, &["--version"]);
                v.as_deref()
                    .and_then(extract_version)
                    .or_else(|| path.as_deref().and_then(read_exe_product_version))
                    .or_else(|| Some("detectado".to_string()))
            } else if let Some(ref p) = path {
                read_exe_product_version(p).or_else(|| Some("detectado".to_string()))
            } else {
                Some("detectado".to_string())
            };
            let latest = match tool.key.as_str() {
                "vscode" => fetch_vscode_latest(),
                "cursor" => fetch_github_latest("getcursor/cursor"),
                "windsurf" => fetch_github_latest("codeium/windsurf"),
                _ => None,
            };
            let has_update = match (&current, &latest) {
                (Some(c), Some(l)) if c != "detectado" => compare_versions(c, l),
                _ => false,
            };
            let no_api = latest.is_none();
            Some(UpdateInfo {
                cli: tool.name.clone(),
                current,
                latest,
                has_update,
                method: "browser".into(),
                no_api,
                key: Some(tool.key.clone()),
            })
        })
        .collect()
}
```

- [ ] **Step 5: Compilar e rodar clippy**

Run: `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`
Expected: sem warnings.

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/util.rs src-tauri/src/commands/cli.rs src-tauri/src/commands/updates.rs
git commit -m "feat(updates): fetch_manifest_version para CLIs script-installed

check_cli_updates agora consulta update_manifest_url quando presente
(Antigravity CLI). check_tool_updates ganha fallback ProductVersion
para IDEs Electron sem --version."
```

---

## Task 7: Frontend — Atualizar tipo `CliInfo` em `useClis.ts`

**Files:**
- Modify: `src/features/launcher/useClis.ts:5-16`

- [ ] **Step 1: Estender a interface**

Em `src/features/launcher/useClis.ts:5-16`, substituir a interface por:

```ts
export interface CliInfo {
  key: string;
  name: string;
  command: string;
  flag: string | null;
  install_cmd: string;
  version_cmd: string;
  npm_pkg: string | null;
  pip_pkg: string | null;
  install_method: string;
  install_url: string | null;
  extra_paths: string[];
  update_manifest_url: string | null;
}
```

- [ ] **Step 2: Verificar tipagem**

Run: `npx tsc --noEmit`
Expected: sem erros novos. Outros consumidores de `CliInfo` ignoram silentemente esses campos.

- [ ] **Step 3: Commit**

```bash
git add src/features/launcher/useClis.ts
git commit -m "feat(useClis): adicionar extra_paths e update_manifest_url à interface"
```

---

## Task 8: Frontend — Hook `useDraggable`

**Files:**
- Create: `src/hooks/useDraggable.ts`

- [ ] **Step 1: Criar o hook**

Criar `src/hooks/useDraggable.ts` com:

```ts
import { useCallback, useState, type DragEvent } from "react";

export interface DragStartHandlers {
  draggable: true;
  onDragStart: (e: DragEvent<HTMLElement>) => void;
  onDragEnd: (e: DragEvent<HTMLElement>) => void;
}

export interface DropHandlers {
  onDragOver: (e: DragEvent<HTMLElement>) => void;
  onDragEnter: (e: DragEvent<HTMLElement>) => void;
  onDragLeave: (e: DragEvent<HTMLElement>) => void;
  onDrop: (e: DragEvent<HTMLElement>) => void;
}

export interface UseDraggable {
  draggedKey: string | null;
  dropTargetKey: string | null;
  startHandlers: (key: string) => DragStartHandlers;
  dropHandlers: (key: string) => DropHandlers;
}

/**
 * Hook para reordenação por drag-and-drop entre cards.
 * Mantém estado local de drag/drop e chama `setOrder` quando o usuário solta.
 *
 * @param orderedKeys lista atual de chaves na ordem visível
 * @param setOrder callback que persiste a nova ordem
 */
export function useDraggable(
  orderedKeys: string[],
  setOrder: (next: string[]) => void,
): UseDraggable {
  const [draggedKey, setDraggedKey] = useState<string | null>(null);
  const [dropTargetKey, setDropTargetKey] = useState<string | null>(null);

  const startHandlers = useCallback(
    (key: string): DragStartHandlers => ({
      draggable: true,
      onDragStart: (e) => {
        setDraggedKey(key);
        e.dataTransfer.effectAllowed = "move";
        e.dataTransfer.setData("text/plain", key);
      },
      onDragEnd: () => {
        setDraggedKey(null);
        setDropTargetKey(null);
      },
    }),
    [],
  );

  const dropHandlers = useCallback(
    (key: string): DropHandlers => ({
      onDragOver: (e) => {
        e.preventDefault();
        e.dataTransfer.dropEffect = "move";
      },
      onDragEnter: (e) => {
        e.preventDefault();
        if (draggedKey && draggedKey !== key) {
          setDropTargetKey(key);
        }
      },
      onDragLeave: (e) => {
        // Só limpa se realmente saiu do alvo (não filhos)
        const related = e.relatedTarget as Node | null;
        if (!related || !(e.currentTarget as Node).contains(related)) {
          setDropTargetKey((curr) => (curr === key ? null : curr));
        }
      },
      onDrop: (e) => {
        e.preventDefault();
        const moved = draggedKey;
        setDropTargetKey(null);
        setDraggedKey(null);
        if (!moved || moved === key) return;
        const oldIdx = orderedKeys.indexOf(moved);
        const newIdx = orderedKeys.indexOf(key);
        if (oldIdx === -1 || newIdx === -1) return;
        const next = [...orderedKeys];
        next.splice(oldIdx, 1);
        next.splice(newIdx, 0, moved);
        setOrder(next);
      },
    }),
    [draggedKey, orderedKeys, setOrder],
  );

  return { draggedKey, dropTargetKey, startHandlers, dropHandlers };
}
```

- [ ] **Step 2: Verificar tipagem**

Run: `npx tsc --noEmit`
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useDraggable.ts
git commit -m "feat(hooks): add useDraggable for card reorder UX

Extrai estado e handlers de DnD da LauncherPage para um hook
reutilizável com suporte a feedback visual (dropTargetKey)."
```

---

## Task 9: Frontend — Refatorar `LauncherPage.tsx` para usar `useDraggable`

**Files:**
- Modify: `src/features/launcher/LauncherPage.tsx` (imports, linhas 74-133, props passadas para cards)

- [ ] **Step 1: Adicionar import**

Em `src/features/launcher/LauncherPage.tsx`, após o import existente de `useUpdates` (linha 14), adicionar:

```ts
import { useDraggable } from "../../hooks/useDraggable";
```

- [ ] **Step 2: Substituir o bloco de DnD (linhas ~74-133)**

Remover completamente este bloco em `src/features/launcher/LauncherPage.tsx:74-133`:

```ts
  const [draggedKey, setDraggedKey] = useState<string | null>(null);
  const [order, setOrder] = useState<string[]>(() => {
    const saved = localStorage.getItem("ai-launcher:cli-order");
    return saved ? JSON.parse(saved) : [];
  });

  const sortedClis = useMemo(() => {
    // ...
  }, [clis, customClis, order]);

  const onDragStart = (e: React.DragEvent, key: string) => { ... };
  const onDragEnd = (e: React.DragEvent) => { ... };
  const onDragOver = (e: React.DragEvent) => { ... };
  const onDrop = (e: React.DragEvent, targetKey: string) => { ... };
```

Substituir por:

```ts
  const [order, setOrderState] = useState<string[]>(() => {
    const saved = localStorage.getItem("ai-launcher:cli-order");
    return saved ? (JSON.parse(saved) as string[]) : [];
  });

  const sortedClis = useMemo(() => {
    const combined: Array<(CliInfo & { isCustom: false }) | (CustomCli & { isCustom: true })> = [
      ...clis.map((c) => ({ ...c, isCustom: false as const })),
      ...customClis.map((c) => ({ ...c, isCustom: true as const })),
    ];
    if (order.length === 0) return combined;
    return [...combined].sort((a, b) => {
      const idxA = order.indexOf(a.key);
      const idxB = order.indexOf(b.key);
      if (idxA === -1 && idxB === -1) return 0;
      if (idxA === -1) return 1;
      if (idxB === -1) return -1;
      return idxA - idxB;
    });
  }, [clis, customClis, order]);

  const orderedKeys = useMemo(() => sortedClis.map((c) => c.key), [sortedClis]);

  const persistOrder = useCallback((next: string[]) => {
    setOrderState(next);
    localStorage.setItem("ai-launcher:cli-order", JSON.stringify(next));
  }, []);

  const { dropTargetKey, startHandlers, dropHandlers } = useDraggable(
    orderedKeys,
    persistOrder,
  );
```

- [ ] **Step 3: Adicionar `useCallback` ao import do React**

No topo de `src/features/launcher/LauncherPage.tsx:1`, mudar:

```ts
import React, { useState, useMemo } from "react";
```

Para:

```ts
import { useState, useMemo, useCallback } from "react";
```

(remove o `React` se ele não for usado em outro lugar — confirmar com `npx tsc --noEmit`).

- [ ] **Step 4: Atualizar a passagem de props aos cards**

Em `src/features/launcher/LauncherPage.tsx`, no map dentro de `{!loading && allCount > 0 && (...)}`, substituir o trecho que monta `<CliCard>` e `<CustomCliCard>` por:

```tsx
          {sortedClis.map((cli) => {
            const isDropTarget = dropTargetKey === cli.key;
            if (!cli.isCustom) {
              const c = cli as CliInfo & { isCustom: false };
              return (
                <CliCard
                  key={c.key}
                  cli={c}
                  check={checks[c.name]}
                  installing={installing === c.key}
                  hasUpdate={cliHasUpdate(c.name)}
                  onLaunch={setLaunching}
                  onInstall={onInstall}
                  startHandlers={startHandlers(c.key)}
                  dropHandlers={dropHandlers(c.key)}
                  isDropTarget={isDropTarget}
                />
              );
            }
            const cc = cli as CustomCli & { isCustom: true };
            return (
              <CustomCliCard
                key={`custom-${cc.key}`}
                cli={cc}
                onLaunch={setLaunchingCustom}
                startHandlers={startHandlers(cc.key)}
                dropHandlers={dropHandlers(cc.key)}
                isDropTarget={isDropTarget}
              />
            );
          })}
```

- [ ] **Step 5: Verificar tipagem**

Run: `npx tsc --noEmit`
Expected: erros apontando para `startHandlers`, `dropHandlers`, `isDropTarget` em `CliCard`/`CustomCliCard` (essas props vão existir nas próximas tasks). Ignorar por ora — vão sumir após Task 10.

- [ ] **Step 6: Commit**

```bash
git add src/features/launcher/LauncherPage.tsx
git commit -m "refactor(launcher): use useDraggable hook for CLI reorder

Substitui 60 linhas de DnD inline pelo hook reutilizável.
Próximas tasks atualizam CliCard/CustomCliCard para o novo contrato."
```

---

## Task 10: Frontend — Atualizar `CliCard.tsx` com drag handle

**Files:**
- Modify: `src/features/launcher/CliCard.tsx`

- [ ] **Step 1: Atualizar a interface de props**

Em `src/features/launcher/CliCard.tsx`, substituir o bloco da interface `CliCardProps` (linhas 15-27) por:

```tsx
import type { DragStartHandlers, DropHandlers } from "../../hooks/useDraggable";

interface CliCardProps {
  cli: CliInfo;
  check?: CheckResult;
  installing?: boolean;
  hasUpdate?: boolean;
  onLaunch: (cli: CliInfo) => void;
  onInstall: (cli: CliInfo) => void;
  startHandlers?: DragStartHandlers;
  dropHandlers?: DropHandlers;
  isDropTarget?: boolean;
}
```

- [ ] **Step 2: Atualizar assinatura e wrapper do componente**

Em `src/features/launcher/CliCard.tsx`, no início da função (linhas 33-46), substituir:

```tsx
export function CliCard({
  cli,
  check,
  installing = false,
  hasUpdate = false,
  onLaunch,
  onInstall,
  draggable,
  onDragStart,
  onDragOver,
  onDrop,
  onDragEnd,
}: CliCardProps) {
```

Por:

```tsx
export function CliCard({
  cli,
  check,
  installing = false,
  hasUpdate = false,
  onLaunch,
  onInstall,
  startHandlers,
  dropHandlers,
  isDropTarget = false,
}: CliCardProps) {
```

- [ ] **Step 3: Atualizar o wrapper de retorno (linhas 107-115)**

Substituir:

```tsx
    <div
      draggable={draggable}
      onDragStart={onDragStart}
      onDragOver={onDragOver}
      onDrop={onDrop}
      onDragEnd={onDragEnd}
      className="cd-draggable-item"
    >
```

Por:

```tsx
    <div
      className={`cd-draggable-item${isDropTarget ? " cd-draggable-item--drop-target" : ""}`}
      {...(dropHandlers ?? {})}
    >
```

- [ ] **Step 4: Adicionar o drag handle dentro do cabeçalho do card**

Em `src/features/launcher/CliCard.tsx`, dentro do `<Card interactive>`, antes da `<div className="cd-cli-card__head">`, NÃO — o handle vai DENTRO do head, como primeiro filho. Substituir a `<div className="cd-cli-card__head">` (linha ~117) inteira pela versão com handle:

Localizar:

```tsx
      <div className="cd-cli-card__head">
        {hasCliIcon(cli.key) ? (
```

Substituir por:

```tsx
      <div className="cd-cli-card__head">
        {startHandlers && (
          <span
            className="cd-drag-handle"
            aria-label={t("launcher.dragToReorder")}
            title={t("launcher.dragToReorder")}
            {...startHandlers}
          >
            ⋮⋮
          </span>
        )}
        {hasCliIcon(cli.key) ? (
```

- [ ] **Step 5: Verificar tipagem**

Run: `npx tsc --noEmit`
Expected: erros em `CustomCliCard.tsx` (próxima task). Erros em `LauncherPage.tsx` devem ter sumido.

- [ ] **Step 6: Commit**

```bash
git add src/features/launcher/CliCard.tsx
git commit -m "feat(CliCard): drag handle dedicado para reordenação

Move o gesto de arrastar para um elemento ⋮⋮ no cabeçalho, evitando
o conflito de mousedown com os botões filhos do Card."
```

---

## Task 11: Frontend — Atualizar `CustomCliCard.tsx` com o mesmo padrão

**Files:**
- Modify: `src/features/launcher/CustomCliCard.tsx`

- [ ] **Step 1: Ler o arquivo atual**

Run: `cat src/features/launcher/CustomCliCard.tsx`

Espera-se interface análoga a `CliCardProps` com `draggable`, `onDragStart`, etc.

- [ ] **Step 2: Substituir a interface e o wrapper**

Aplicar as mesmas substituições da Task 10 em `CustomCliCard.tsx`:

- Import `DragStartHandlers` e `DropHandlers` de `../../hooks/useDraggable`.
- Substituir props `draggable`, `onDragStart`, `onDragOver`, `onDrop`, `onDragEnd` por `startHandlers`, `dropHandlers`, `isDropTarget`.
- Wrapper externo com `className` condicional + spread `dropHandlers`.
- Drag handle `⋮⋮` dentro do head do card, condicional em `startHandlers`.

Se o arquivo não tiver `useTranslation`, adicionar:

```tsx
import { useTranslation } from "react-i18next";
// ...
const { t } = useTranslation();
```

- [ ] **Step 3: Verificar tipagem**

Run: `npx tsc --noEmit`
Expected: sem erros.

- [ ] **Step 4: Commit**

```bash
git add src/features/launcher/CustomCliCard.tsx
git commit -m "feat(CustomCliCard): drag handle dedicado consistente com CliCard"
```

---

## Task 12: Frontend — CSS para drag handle e drop target

**Files:**
- Modify: `src/features/launcher/LauncherPage.css:250-268`

- [ ] **Step 1: Substituir o bloco de drag-and-drop CSS**

Em `src/features/launcher/LauncherPage.css:250-268`, substituir:

```css
.cd-draggable-item {
  cursor: grab;
  transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.2s ease;
  user-select: none;
  touch-action: none;
}

.cd-draggable-item:active {
  cursor: grabbing;
}

.cd-draggable-item--dragging {
  opacity: 0.4;
  transform: scale(0.98);
}

.cd-draggable-item:hover:not(.cd-draggable-item--dragging) {
  transform: translateY(-2px);
}
```

Por:

```css
.cd-draggable-item {
  transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.2s ease, outline-color 0.15s ease;
}

.cd-draggable-item:hover {
  transform: translateY(-2px);
}

.cd-draggable-item--drop-target {
  outline: 2px dashed var(--accent);
  outline-offset: -4px;
  transform: translateY(0);
}

.cd-drag-handle {
  cursor: grab;
  opacity: 0.25;
  padding: 2px 6px;
  font-size: 14px;
  line-height: 1;
  letter-spacing: -3px;
  user-select: none;
  color: var(--text-dim);
  transition: opacity 0.15s ease;
  flex-shrink: 0;
}

.cd-draggable-item:hover .cd-drag-handle {
  opacity: 0.7;
}

.cd-drag-handle:active {
  cursor: grabbing;
  opacity: 1;
}
```

- [ ] **Step 2: Smoke visual via dev server**

Run: `npm run tauri dev`
Expected: app abre, dashboard carrega, cards mostram o handle `⋮⋮` à esquerda do ícone com opacidade baixa que sobe ao hover. Tente arrastar um card pelo handle. A ordem deve persistir após reload.

Encerre o dev server com `Ctrl+C` depois do smoke.

- [ ] **Step 3: Commit**

```bash
git add src/features/launcher/LauncherPage.css
git commit -m "style(launcher): drag handle e drop-target outline

Remove cursor:grab do card inteiro (limitado ao handle agora).
Adiciona feedback visual de drop-target com outline tracejado."
```

---

## Task 13: Frontend — Remover refs a Gemini do registry e da UI

**Files:**
- Modify: `src/lib/iconRegistry.ts:11`
- Modify: `src/icons/registry.ts:1, 12-22`
- Modify: `src/features/launcher/LaunchDialog.tsx:29`
- Modify: `src/features/help/AnimatedTerminal.tsx:73, 99`

- [ ] **Step 1: `src/lib/iconRegistry.ts`**

Em `src/lib/iconRegistry.ts:11`, substituir:

```ts
const CLI_KEYS = ["claude", "codex", "gemini", "qwen", "kilocode", "opencode", "crush", "droid"];
```

Por:

```ts
const CLI_KEYS = ["claude", "codex", "qwen", "kilocode", "opencode", "crush", "droid", "antigravity"];
```

- [ ] **Step 2: `src/icons/registry.ts`**

Em `src/icons/registry.ts:1`, substituir:

```ts
const CLI_PNG = new Set(["claude", "codex", "gemini", "qwen"]);
```

Por:

```ts
const CLI_PNG = new Set(["claude", "codex", "qwen"]);
```

Em `src/icons/registry.ts:12-22`, remover `"gemini"` da lista. Antes:

```ts
const CLI_KEYS = new Set([
  "claude",
  "codex",
  "gemini",
  "qwen",
  "crush",
  "droid",
  "kilocode",
  "opencode",
  "antigravity",
]);
```

Depois:

```ts
const CLI_KEYS = new Set([
  "claude",
  "codex",
  "qwen",
  "crush",
  "droid",
  "kilocode",
  "opencode",
  "antigravity",
]);
```

- [ ] **Step 3: `src/features/launcher/LaunchDialog.tsx`**

Em `src/features/launcher/LaunchDialog.tsx:29`, substituir:

```ts
const CLIS_WITH_PROMPT_FLAG = new Set(["claude", "codex", "gemini", "antigravity"]);
```

Por:

```ts
const CLIS_WITH_PROMPT_FLAG = new Set(["claude", "codex", "antigravity"]);
```

- [ ] **Step 4: `src/features/help/AnimatedTerminal.tsx`**

Localizar e remover as linhas que referenciam Gemini:

Run: `grep -n "Gemini" src/features/help/AnimatedTerminal.tsx`

Expected: 2 ocorrências (linhas ~73 e ~99).

Em `src/features/help/AnimatedTerminal.tsx:73`, remover a linha que contém:

```tsx
      out(ok("  ✓ "), plain("Gemini CLI v1.2         "), dim("[installed]")),
```

Em `src/features/help/AnimatedTerminal.tsx:99`, remover a linha que contém:

```tsx
      out(dim("  Gemini CLI    "), plain("$0.92  "), dim("(5 sessions)")),
```

- [ ] **Step 5: Verificar tipagem e build**

Run: `npx tsc --noEmit`
Expected: sem erros.

Run: `npm run build`
Expected: build sucesso.

- [ ] **Step 6: Deletar ícones do Gemini**

Run: `git rm public/icons/cli/gemini.svg public/icons/cli/gemini.png`
Expected: 2 arquivos staged para remoção.

- [ ] **Step 7: Commit**

```bash
git add src/lib/iconRegistry.ts src/icons/registry.ts src/features/launcher/LaunchDialog.tsx src/features/help/AnimatedTerminal.tsx
git commit -m "chore: remover Gemini do registry, dialogs e terminal animado

Gemini CLI foi descontinuado (substituído por Antigravity via 'agy').
Remove referências de UI e ícones associados."
```

---

## Task 14: Frontend — Strings i18n

**Files:**
- Modify: `src/i18n/locales/en.ts`
- Modify: `src/i18n/locales/pt-BR.ts`

- [ ] **Step 1: Atualizar `en.ts`**

Em `src/i18n/locales/en.ts:86`, substituir:

```ts
"We'll look for Claude, Codex, Gemini, and the other supported CLIs on your PATH. Nothing is uploaded.",
```

Por:

```ts
"We'll look for Claude, Codex, Antigravity, and the other supported CLIs on your PATH. Nothing is uploaded.",
```

Em `src/i18n/locales/en.ts:202`, substituir:

```ts
"No usage data. Run a supported CLI (Claude Code, Codex, Gemini) at least once.",
```

Por:

```ts
"No usage data. Run a supported CLI (Claude Code, Codex, Antigravity) at least once.",
```

Em `src/i18n/locales/en.ts:211`, substituir:

```ts
"AI Launcher Pro detects and manages AI coding CLIs (Claude, Codex, Gemini, and more) ...",
```

Por:

```ts
"AI Launcher Pro detects and manages AI coding CLIs (Claude, Codex, Antigravity, and more) ...",
```

Em `src/i18n/locales/en.ts:239`, substituir:

```ts
"Usage is read from each CLI's local log files. Run a supported CLI (Claude Code, Codex, Gemini) at least once so those files are created.",
```

Por:

```ts
"Usage is read from each CLI's local log files. Run a supported CLI (Claude Code, Codex, Antigravity) at least once so those files are created.",
```

Acrescentar dentro da chave `launcher` (em qualquer posição estável, p.ex. logo após `launch`):

```ts
      dragToReorder: "Drag to reorder",
```

- [ ] **Step 2: Atualizar `pt-BR.ts`**

Em `src/i18n/locales/pt-BR.ts:84`, substituir `Gemini` por `Antigravity`. Mesma substituição nas linhas 200, 209, 238.

Acrescentar dentro de `launcher`:

```ts
      dragToReorder: "Arrastar para reordenar",
```

- [ ] **Step 3: Verificar tipagem**

Run: `npx tsc --noEmit`
Expected: sem erros.

- [ ] **Step 4: Commit**

```bash
git add src/i18n/locales/en.ts src/i18n/locales/pt-BR.ts
git commit -m "i18n: substituir Gemini por Antigravity e adicionar dragToReorder"
```

---

## Task 15: Smoke test end-to-end no dev server

**Files:** nenhum.

- [ ] **Step 1: Iniciar o dev server**

Run: `npm run tauri dev`
Expected: app abre sem erros no console.

- [ ] **Step 2: Verificar aba Launcher**

- Card "Antigravity" aparece com chip verde "1.0.0" (ou versão atual do `agy --version`).
- Não há card "Gemini" em nenhum lugar.
- Cada card mostra `⋮⋮` no canto superior esquerdo do head, opacidade baixa (~25%) subindo para ~70% ao hover sobre o card.

- [ ] **Step 3: Verificar aba Tools**

- Card "Antigravity" aparece com chip verde "2.0.0" (ProductVersion do `.exe`).

- [ ] **Step 4: Testar drag-and-drop**

- Clicar+segurar no `⋮⋮` de um card e arrastar sobre outro: card alvo recebe outline tracejado (`--drop-target`).
- Soltar: ordem dos cards muda. Recarregar a janela (`Ctrl+R` na DevTools): a nova ordem persiste.

- [ ] **Step 5: Verificar updates**

- Abrir aba Updates (ou clicar em "Verificar atualizações"). O card do Antigravity CLI deve mostrar `latest` = versão do manifesto (ex: `1.0.0`) sem erro.

- [ ] **Step 6: Encerrar dev server**

`Ctrl+C` no terminal.

- [ ] **Step 7: Se algum passo falhou**, registrar em `.wolf/buglog.json` (próxima task agrupa todos os updates de memória).

---

## Task 16: Build local de produção

**Files:** nenhum.

- [ ] **Step 1: Build do bundle web**

Run: `npm run build`
Expected: sucesso, `dist-public/` e `dist-admin/` atualizados.

- [ ] **Step 2: Build do app Tauri**

Run: `npm run tauri build`
Expected: gera instalador `.msi` ou `.exe` em `src-tauri/target/release/bundle/`. Não publicar nada.

- [ ] **Step 3: Smoke test do instalador**

- Localizar o `.msi`/`.exe` produzido.
- (Opcional) Instalar localmente em uma pasta temporária e abrir.
- Repetir verificações da Task 15.

- [ ] **Step 4: Sem commit aqui** — artefatos de build estão no `.gitignore`.

---

## Task 17: Atualizar CHANGELOG, cerebrum e buglog

**Files:**
- Modify: `CHANGELOG.md` (substituir entrada esboço v15.2.0)
- Modify: `.wolf/cerebrum.md`
- Modify: `.wolf/buglog.json`

- [ ] **Step 1: Substituir entrada v15.2.0 no CHANGELOG**

Em `CHANGELOG.md`, localizar o bloco `## [15.2.0] — 2026-05-19 — Antigravity Fixes & Drag-and-Drop` e substituí-lo (do título até a linha antes de `## [15.1.0]`) por:

```markdown
## [15.2.0] — 2026-05-19 — Antigravity CLI Real + Electron Versioning + DnD Fix

Release focada em três correções convergentes: a detecção real do Antigravity CLI, a versão do IDE Electron e a usabilidade do drag-and-drop na Dashboard.

### Added
- **Antigravity CLI (binário `agy`)** — Detecção via `%LOCALAPPDATA%\agy\bin\agy.exe`, instalação por script PowerShell oficial e checagem de updates via manifesto JSON público do Google.
- **`extra_paths` e `update_manifest_url`** — Novos campos opcionais em `CliInfo` permitindo CLIs script-installed sem hardcode.
- **`read_exe_product_version`** — Lê `ProductVersion` do PE para IDEs Electron (Antigravity, Cursor, Windsurf) que não respondem a `--version`.
- **Drag handle dedicado** — Elemento `⋮⋮` no cabeçalho dos CLI cards com cursor `grab`, opacidade animada e outline de drop-target.
- **Hook `useDraggable`** — Lógica reutilizável de DnD entre cards.

### Changed
- **Antigravity CLI** migrado de pacote npm fake `@google/antigravity` para o binário oficial `agy` instalado via `iwr https://antigravity.google/cli/install.ps1 -useb | iex`.
- **`check_tools` e `check_tool_updates`** caem em `ProductVersion` quando `--version` não retorna versão extraível.
- **DnD desacoplado do Card** — só o handle dispara `dragStart`, eliminando o conflito com `mousedown` dos botões filhos.

### Removed
- **Gemini CLI** — Descontinuado pelo Google; substituído pelo Antigravity em todas as listas, registries e copy.
- **Hardcode `antigravity`** em `resolve_windows_cmd` (binário real é `.exe`, não `.cmd`).
- **Ícones `gemini.svg`/`gemini.png`** em `public/icons/cli/`.

### Fixed
- **Antigravity não detectado** — comando real é `agy`, não `antigravity`; pacote `@google/antigravity` retorna 404 no npm.
- **Versão do Antigravity IDE ausente** — Electron apps não respondem a `--version`; agora lemos `ProductVersion`.
- **Drag-and-drop não iniciava** — `<Button>`/`<Chip>` consumiam `mousedown` antes do drag; handle dedicado resolve.
```

- [ ] **Step 2: Atualizar `.wolf/cerebrum.md`**

Abrir `.wolf/cerebrum.md` e adicionar:

Sob `## Key Learnings`:

```markdown
- **Antigravity CLI real**: o binário se chama `agy`, NÃO `antigravity`. Instalado por script PowerShell em `%LOCALAPPDATA%\agy\bin\agy.exe`. Não existe pacote npm — `@google/antigravity` retorna 404.
- **Manifesto de update do agy**: `https://antigravity-cli-auto-updater-974169037036.us-central1.run.app/manifests/windows_amd64.json` — campo `.version` é SemVer.
- **IDEs Electron não respondem a `--version`** (Antigravity, Cursor, Windsurf). Para a versão real, ler `ProductVersion` do PE via `(Get-Item).VersionInfo.ProductVersion` ou WinAPI.
- **DnD em Cards interativos**: aplicar `draggable` no wrapper que contém botões NÃO funciona porque os filhos consomem `mousedown`. Solução: handle dedicado.
```

Sob `## Decision Log`:

```markdown
- **2026-05-19** — Generalizar `CliInfo` com `extra_paths`+`update_manifest_url` em vez de hardcode por CLI. Razão: o droid já é script-installed; agy seria o segundo caso — abstrair foi mais barato que duplicar.
- **2026-05-19** — Ler `ProductVersion` via PowerShell em vez de crate `windows` nativa. Razão: 4 linhas vs nova dep; ainda dentro do orçamento de latência (~200ms cacheado).
- **2026-05-19** — Drag handle visível em vez de drag invisível no wrapper. Razão: affordance + zero conflito de `mousedown` com botões filhos.
- **2026-05-19** — Update do IDE Antigravity adiado para v15.3. Razão: endpoint web 404 nos paths comuns; investigação requer captura de tráfego do app rodando.
```

Sob `## Do-Not-Repeat`:

```markdown
- **[2026-05-19]** Adicionar CLI nova: SEMPRE validar o nome do binário e a existência do pacote npm rodando `where <cmd>` e `npm view <pkg> version` na máquina real antes de commitar. A entrada original do Antigravity ficou quebrada porque os campos foram preenchidos sem validação.
- **[2026-05-19]** Para IDEs Electron, NUNCA confiar em `<exe> --version` — Electron mostra um dialog/no-op. Ler `ProductVersion` direto do PE.
- **[2026-05-19]** Drag-and-drop dentro de `<Card>` com botões: NÃO aplicar `draggable` no wrapper externo. Usar um handle dedicado isolado dos botões.
```

- [ ] **Step 3: Atualizar `.wolf/buglog.json`**

Ler o JSON atual:

Run: `cat .wolf/buglog.json`

Se for um array, apender; se for `{"bugs": [...]}`, apender ao `bugs`. Caso o arquivo não exista ou esteja vazio, criar com:

```json
{
  "bugs": [
    {
      "id": "bug-001",
      "timestamp": "2026-05-19T00:00:00-03:00",
      "error_message": "Antigravity CLI não é detectado mesmo quando instalado",
      "file": "src-tauri/src/util.rs",
      "root_cause": "Entrada do Antigravity em get_cli_definitions apontava para o pacote npm inexistente '@google/antigravity' (404 no registry) e para o comando 'antigravity', mas o binário real é 'agy' em %LOCALAPPDATA%\\agy\\bin\\agy.exe instalado por script PowerShell oficial.",
      "fix": "Estender CliInfo com extra_paths e update_manifest_url; substituir entrada antigravity para usar command='agy', install_method='script', install_cmd=iwr ... | iex, extra_paths=[%LOCALAPPDATA%\\agy\\bin\\agy.exe], update_manifest_url=https://antigravity-cli-auto-updater-.../manifests/windows_amd64.json.",
      "tags": ["antigravity", "cli", "detection", "rust", "v15.2"],
      "related_bugs": [],
      "occurrences": 1,
      "last_seen": "2026-05-19T00:00:00-03:00"
    },
    {
      "id": "bug-002",
      "timestamp": "2026-05-19T00:00:00-03:00",
      "error_message": "Antigravity IDE detectado na aba Tools mas sem versão",
      "file": "src-tauri/src/commands/tools.rs",
      "root_cause": "Electron apps (Antigravity, Cursor, Windsurf) não respondem a 'antigravity --version'. O check_tools chamava o binário com --version e descartava o resultado vazio.",
      "fix": "Adicionar fallback read_exe_product_version(path) que lê ProductVersion do PE via PowerShell (Get-Item).VersionInfo.ProductVersion. Aplicado em check_tools e check_tool_updates.",
      "tags": ["antigravity", "ide", "electron", "version", "v15.2"],
      "related_bugs": ["bug-001"],
      "occurrences": 1,
      "last_seen": "2026-05-19T00:00:00-03:00"
    },
    {
      "id": "bug-003",
      "timestamp": "2026-05-19T00:00:00-03:00",
      "error_message": "Drag-and-drop dos cards de CLI na Dashboard não funcionava",
      "file": "src/features/launcher/CliCard.tsx",
      "root_cause": "A prop draggable estava no wrapper externo do <Card>, mas o Card contém <Button> e <Chip> que consomem mousedown antes do drag iniciar. O gesto nunca disparava onDragStart.",
      "fix": "Extrair lógica para hook useDraggable e mover draggable para um elemento dedicado ⋮⋮ (drag handle) no cabeçalho do card, isolado dos botões. Adicionar feedback visual de drop-target (outline tracejado).",
      "tags": ["dnd", "ui", "react", "launcher", "v15.2"],
      "related_bugs": [],
      "occurrences": 1,
      "last_seen": "2026-05-19T00:00:00-03:00"
    }
  ]
}
```

(Se já existirem bugs no arquivo, adicionar estes três ao array `bugs`, renumerando os IDs para evitar colisão.)

- [ ] **Step 4: Atualizar `.wolf/anatomy.md`**

Adicionar (append) o novo arquivo na seção `## src/hooks/`:

```markdown
- `useDraggable.ts` — Exports useDraggable, DragStartHandlers, DropHandlers; estado e handlers de DnD entre cards (~470 tok)
```

- [ ] **Step 5: Commit final**

```bash
git add CHANGELOG.md .wolf/cerebrum.md .wolf/buglog.json .wolf/anatomy.md
git commit -m "docs: atualizar CHANGELOG v15.2 final + cerebrum + buglog

Registra as três correções estruturais: comando real do Antigravity
('agy'), leitura de ProductVersion para Electron, e drag handle
dedicado. Decision Log e Do-Not-Repeat documentam o aprendizado."
```

---

## Self-Review

**Cobertura do spec:**
- §3.1 (CliInfo + agy + remover gemini) → Tasks 1, 2 ✅
- §3.1 (`resolve_windows_cmd`, `resolve_cli_path_win`, `extra_paths`) → Task 3 ✅
- §3.1 (`read_exe_product_version`) → Task 4 ✅
- §3.2 (fallback em `check_tools`) → Task 5 ✅
- §3.3 (`update_manifest_url` em check_cli_updates + fallback em check_tool_updates) → Task 6 ✅
- §3.4 (hook `useDraggable`) → Task 8 ✅
- §3.5 (`CliCard`/`CustomCliCard` drag handle) → Tasks 10, 11 ✅
- §3.6 (CSS) → Task 12 ✅
- §3.7 (i18n) → Task 14 ✅
- §3.8 (icon registry — verificação) → Task 13 ✅
- §3.9 (CHANGELOG) → Task 17 ✅
- §3.10 (cerebrum + buglog) → Task 17 ✅
- §4 (testes) → Tasks 1, 2, 3, 4 (unit) + 15 (manual) ✅
- §6 (riscos `--yolo`) → Task 2 Step 6 ✅
- §7 (critério de aceitação) → Tasks 15, 16 ✅
- Plus: refactor `LauncherPage.tsx` → Task 9; rebump versão já está em 15.2.0 (sem task necessária).

**Placeholders:** Nenhum "TODO", "TBD", "implement later" no plano.

**Consistência de tipos:**
- `DragStartHandlers`, `DropHandlers`, `UseDraggable` — definidos na Task 8, usados nas Tasks 10/11 com mesmos nomes ✅
- `CliInfo` Rust e TS — campos `extra_paths`, `update_manifest_url` consistentes ✅
- `resolve_cli_path_win(cmd: &str, extra_paths: &[String])` — assinatura usada idêntica nos três call sites (Task 3 Step 3) ✅

Plano OK para execução.

---

**Plan complete and saved to `docs/superpowers/plans/2026-05-19-v15.2-antigravity-cli-plan.md`. Two execution options:**

**1. Subagent-Driven (recommended)** - Dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
