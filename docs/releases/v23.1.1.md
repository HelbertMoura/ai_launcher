# AI Launcher v23.1.1

The bug-fix release: top-bar dropdowns paint above the page again — they had been opening invisibly behind the main content since v22.5.0 — and MCP stdio health stops reporting "unavailable" for executables that resolve outside the GUI process PATH. Switched-off MCP servers get an honest "Disabled" state with its own overview metric, MCP configuration failures show a friendly localized message instead of raw Zod output, and the MCP card layout stops squeezing server names and scrolling sideways.

## Highlights

- **Top-bar dropdowns fixed (regression since v22.5.0)**: the notifications bell and quick-settings dropdowns opened invisible — clicks landed on the page and both buttons felt dead — because the top bar carried `contain: layout`, whose stacking context trapped the absolutely-positioned flyouts while the sibling main area painted after it. The containment is removed so the flyouts paint above the page again, and a real hit-test regression guard covers both panels in e2e: `document.elementFromPoint` at the open panel's center must return the panel or a descendant, because `toBeVisible()` cannot catch paint-order bugs.
- **Honest stdio health outside PATH**: path-form commands are validated by direct file existence after `%VAR%` expansion (trying `.exe`/`.cmd`/`.bat` variants), and bare names are additionally looked up in the common tool install dirs that never touch PATH — npm folders, `~\.local\bin`, `~\.cargo\bin`, scoop shims, and Python Scripts. The health tooltip carries the exact failure reason. Honest scope, unchanged from the feature: stdio health verifies executable presence, not a protocol handshake.
- **Disabled servers are first-class**: servers with `enabled == false` are never probed anymore — they answer a neutral "Disabled" state instead of a misleading "unavailable", and the MCP overview gains a fifth metric column for them (Healthy / Unavailable / Unknown / Disabled / CLIs).
- **Friendly config errors**: MCP configuration load failures render a localized banner instead of the raw Zod issue list; the technical detail goes to the console (`console.warn`).
- **MCP card layout**: the server name sits alone on its own row (no longer squeezed to a few characters, badges moved to a second row), command boxes wrap with word-break instead of a horizontal scrollbar, and env/header lists scroll within a max height.

## Validation

v23.1.1 validation on the release commit:

- `npm run typecheck` (0 errors)
- `npm test` (330 tests passing)
- `cargo test` (198 tests passing, 1 ignored)
- Playwright e2e suite (75 tests, including the new top-bar hit-test regression guard and the MCP wave-B suites on the `{servers, warnings}` stub contract)
- Playwright screenshot capture suite re-run for the README assets: all 8 captures refreshed on v23.1.1 (the MCP one shows the fixed card layout with the server name on its own row)

## Expected release assets

The GitHub release must attach:

- [ ] `AI.Launcher_23.1.1_x64_en-US.msi` (Windows, WiX)
- [ ] `AI.Launcher_23.1.1_x64-setup.exe` (Windows, NSIS)
- [ ] `AI.Launcher_23.1.1_aarch64.dmg` (macOS, Apple silicon)
- [ ] `AI.Launcher_23.1.1_x64.dmg` (macOS, Intel)
- [ ] `ai-launcher_23.1.1_amd64.AppImage` (Linux)
- [ ] `ai-launcher_23.1.1_amd64.deb` (Linux)
- [ ] `latest.json` (updater manifest, signed)

Each artifact gets its SHA256 published in the release notes; `latest.json` must carry a non-empty updater signature (checked by `scripts/audit-release.sh`).

## Updater note

The fail-closed release pipeline introduced in v22.9.0 remains in force and is proven on this release: if `TAURI_SIGNING_PRIVATE_KEY` is not configured or the generated `.sig` is missing, the workflow aborts before publishing `latest.json`, and `scripts/audit-release.sh` re-checks that the published manifest carries a non-empty signature. An unsigned or empty-signature manifest cannot reach users, which protects the in-app updater from bricking itself with an update it would refuse to install.

## Notes

- All operations remain 100% local, offline-first, and private. No cloud sync, telemetry, or remote tracking.
- Windows remains the first-class platform; macOS and Linux builds ship the same core with platform-native secure storage and fail-closed gating for anything not yet available outside Windows.
- v23.1.1 is a patch release: no schema migrations, no storage format changes, and no new surfaces — the fixes repair behavior introduced or exposed by earlier releases.
- stdio MCP health continues to verify executable presence, not a protocol handshake; end-to-end protocol probing remains future work, as documented when the feature shipped.
