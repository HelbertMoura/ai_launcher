/**
 * Shared Phosphor icon re-exports for the Command Deck design system.
 *
 * Use these imports instead of emojis or ad-hoc Unicode symbols in UI chrome.
 * Pick `regular` weight by default (matches Command Deck sharpness); use
 * `bold` when an icon needs extra presence.
 *
 * IMPORTANT: Do not use these icons to replace the intentional ASCII glyphs
 * used for status dots (●/○), timeline ticks (✓/✗/✕), sidebar dividers,
 * or the terminal empty-state art. Those are part of the visual language.
 */
export {
  Rocket,
  Gear,
  ChartBar,
  Wrench,
  ArrowUp,
  ArrowDown,
  Check,
  X,
  Warning,
  Package,
  Target,
  CurrencyDollar,
  House,
  Folder,
  FolderOpen,
  FileText,
  Key,
  ShieldCheck,
  TestTube,
  Clock,
  Palette,
  Bell,
  FloppyDisk,
  TrendUp,
  Lightning,
  MagnifyingGlass,
  Keyboard,
  Desktop,
  Terminal,
  Download,
  Upload,
  Copy,
  PencilSimple,
  Trash,
  Plus,
  DotsThreeVertical,
  Moon,
  Sun,
  Play,
  Pause,
  Stop,
  ArrowClockwise,
  Info,
  Question,
  Star,
  PushPin,
  Eye,
  EyeSlash,
} from "@phosphor-icons/react";
